# AGENT_CAPABILITY_MATRIX.md

William / Jarvis Multi-Agent AI SaaS System — Digital Promotix

Auto-generated from the live source of truth at `agents/capability_manifest.py` and `agents/capability_data/*.py` (14 agents × 50 capabilities = 700 entries). This is the same data served live by `GET /api/v1/agents` (`agent.capability_manifest` / `agent.capability_manifest_meta` per agent) and seeded into the real `agent_registry` DB table via `python -m database.seeders.seed_agent_capabilities`.

## Summary

| Agent | Purpose | Total | Available | Needs external dep. | Approval required | Planned | Unavailable | Test prompt |
|---|---|---|---|---|---|---|---|---|
| **Voice Agent** | Speaking, listening, STT, TTS, multilingual conversation, audio routing, and voice-controlled task execution. | 50 | 23 | 22 | 3 | 2 | 0 | _Read me my latest notifications and reply to the urgent one._ |
| **System Agent** | Device/app/file/OS control with strict approval and safe local workers. | 50 | 23 | 25 | 1 | 1 | 0 | _Check my CPU, RAM, and disk usage right now._ |
| **Browser Agent** | Internet research, public page extraction, SEO, competitor analysis, safe browser automation. | 50 | 43 | 7 | 0 | 0 | 0 | _Research our top 3 competitors' pricing pages and summarize their plans._ |
| **Code Agent** | Build, edit, debug, test, and deploy code safely. | 50 | 41 | 2 | 7 | 0 | 0 | _Add input validation to the login form and write a unit test for it._ |
| **Memory Agent** | Short/long/project/client/team memory with privacy, recall, and workspace isolation. | 50 | 36 | 2 | 9 | 3 | 0 | _Remember that our client Acme Corp prefers email over calls._ |
| **Security Agent** | Risk, approval, policy, audit, fraud, anomaly, and permission brain. | 50 | 45 | 2 | 3 | 0 | 0 | _Show me all pending approval requests from the last 24 hours._ |
| **Verification Agent** | Confirm task results with state checks, command output, screenshots, validation, retries, and proof. | 50 | 44 | 6 | 0 | 0 | 0 | _Verify that the last deployment actually succeeded._ |
| **Visual Agent** | Screen/image/video understanding, OCR, UI mapping, privacy filtering, and visual validation. | 50 | 18 | 32 | 0 | 0 | 0 | _Look at this screenshot and tell me what error is being shown._ |
| **Workflow Agent** | Automation pipelines, webhooks, triggers, n8n, forms, sheets, WhatsApp, CRM, scheduling, monitoring. | 50 | 35 | 14 | 1 | 0 | 0 | _Build a workflow that adds new form submissions to our CRM and sends a WhatsApp alert._ |
| **Hologram Agent** | AR/glasses/spatial overlay brain with safe structured output when hardware is unavailable. | 50 | 42 | 7 | 1 | 0 | 0 | _Generate an AR overlay card showing today's top 3 tasks._ |
| **Call Agent** | AI receptionist, call consent, lead qualification, summaries, CRM handoff, appointment booking. | 50 | 39 | 4 | 7 | 0 | 0 | _Draft a receptionist script for after-hours calls and qualify this lead transcript._ |
| **Business Agent** | CRM, leads, clients, pipeline, reports, campaign analytics, business intelligence. | 50 | 46 | 1 | 3 | 0 | 0 | _Give me this week's business report: pipeline status, hot leads, and follow-ups due._ |
| **Finance Agent** | Safe finance preparation: invoices, budgets, receipts, transaction drafts only, never auto-transfer. | 50 | 45 | 2 | 3 | 0 | 0 | _Draft an invoice for client Acme Corp for $2,400 due in 30 days._ |
| **Creator Agent** | Content/video production brain: scripts, VEO prompts, captions, thumbnails, calendars, campaign creative. | 50 | 49 | 0 | 1 | 0 | 0 | _Write a 30-second video ad script for our new product launch with 3 hook variations._ |

**Total capabilities across all 14 agents: 700**

Status legend: **Available** = real local implementation working today. **Configured** = works today given already-present config. **Needs external dependency** = returns a structured `external_dependency_required` response until an API key/hardware/worker is connected (never fakes success). **Approval required** = routes through Security Agent before executing. **Planned** = future/hardware-only, no adapter path yet. **Unavailable** = cannot exist safely in this architecture.

---

## Voice Agent (`voice`)

**Purpose:** Speaking, listening, STT, TTS, multilingual conversation, audio routing, and voice-controlled task execution.

**Example test prompt:** _Read me my latest notifications and reply to the urgent one._

| # | Name | Risk | Permission | Status | Required integrations | Audit | Safe MVP behavior | Verification method | Memory policy |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Push-to-talk command capture | medium | allowed | Needs external dependency | audio_input_worker, stt_provider_api_key | No | Accepts a typed command today; audio capture returns external_dependency_required until an audio input worker and STT provider are configured. | VerificationAgent confirms the reported adapter/config state matches the actual configured integration. | Not persisted; operates on transient audio/text without creating a durable record. |
| 2 | Wake word detection for "William" | medium | allowed | Needs external dependency | wake_word_engine, audio_input_worker | No | Wake-word listening stays off until a wake-word engine is configured; text commands work without it. | VerificationAgent confirms the reported adapter/config state matches the actual configured integration. | Not persisted; operates on transient audio/text without creating a durable record. |
| 3 | Custom wake phrase profiles | medium | allowed | Needs external dependency | wake_word_engine | Yes | Stores the requested phrase text in the DB now; live phrase detection requires a configured wake-word engine. | VerificationAgent confirms the reported adapter/config state matches the actual configured integration. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 4 | Always-listening idle mode with privacy disabled by default | high | blocked_by_default | Approval required | wake_word_engine, audio_input_worker | Yes | Refuses to enable continuous listening unless the user explicitly opts in via an approved config change. | VerificationAgent confirms a matching audit log row was written for this action. | Not persisted; operates on transient audio/text without creating a durable record. |
| 5 | No private audio storage by default | low | allowed | Available | — | Yes | Enforces a hard guard that discards captured audio buffers after transcription instead of persisting them. | VerificationAgent confirms a matching audit log row was written for this action. | Not persisted; operates on transient audio/text without creating a durable record. |
| 6 | Streaming speech-to-text | low | allowed | Needs external dependency | stt_provider_api_key | No | Returns external_dependency_required until a streaming STT provider is configured; text input works today. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on transient audio/text without creating a durable record. |
| 7 | Offline STT fallback adapter | low | allowed | Needs external dependency | offline_stt_model | No | Returns external_dependency_required until an offline STT model is bundled/configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on transient audio/text without creating a durable record. |
| 8 | Text-to-speech response | low | allowed | Needs external dependency | tts_provider_api_key | No | Returns the reply as text now; audio playback requires a configured TTS provider. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on transient audio/text without creating a durable record. |
| 9 | Streaming TTS | low | allowed | Needs external dependency | tts_provider_api_key | No | Returns external_dependency_required until a streaming-capable TTS provider is configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on transient audio/text without creating a durable record. |
| 10 | Interrupt TTS when user speaks | low | allowed | Needs external dependency | stt_provider_api_key, tts_provider_api_key | No | Requires a live STT+TTS pipeline to detect barge-in; returns external_dependency_required until both are configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on transient audio/text without creating a durable record. |
| 11 | Smart silence detection | low | allowed | Needs external dependency | voice_activity_detection_adapter | No | Returns external_dependency_required until a voice-activity-detection adapter is configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on transient audio/text without creating a durable record. |
| 12 | Noise suppression adapter | low | allowed | Needs external dependency | noise_suppression_sdk | No | Returns external_dependency_required until a noise-suppression SDK is configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on transient audio/text without creating a durable record. |
| 13 | Echo cancellation adapter | low | allowed | Needs external dependency | echo_cancellation_sdk | No | Returns external_dependency_required until an echo-cancellation SDK is configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on transient audio/text without creating a durable record. |
| 14 | Whisper input detection | low | allowed | Needs external dependency | stt_provider_api_key | No | Returns external_dependency_required until the STT provider's low-volume/whisper mode is configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on transient audio/text without creating a durable record. |
| 15 | Whisper response mode | low | allowed | Needs external dependency | tts_provider_api_key | No | Returns external_dependency_required until a TTS provider supporting a low-volume voice style is configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on transient audio/text without creating a durable record. |
| 16 | Roman Urdu + English mixed command parsing | low | allowed | Available | — | No | Parses the already-received text command using rule-based mixed-language tokenization. | VerificationAgent confirms response matches the normalized result schema. | Held only for the lifetime of the active session/turn; not persisted to durable storage. |
| 17 | Urdu understanding | low | allowed | Available | — | No | Interprets the received text command in Urdu using the core language-understanding pipeline. | VerificationAgent confirms response matches the normalized result schema. | Held only for the lifetime of the active session/turn; not persisted to durable storage. |
| 18 | Hindi understanding | low | allowed | Available | — | No | Interprets the received text command in Hindi using the core language-understanding pipeline. | VerificationAgent confirms response matches the normalized result schema. | Held only for the lifetime of the active session/turn; not persisted to durable storage. |
| 19 | Arabic understanding | low | allowed | Available | — | No | Interprets the received text command in Arabic using the core language-understanding pipeline. | VerificationAgent confirms response matches the normalized result schema. | Held only for the lifetime of the active session/turn; not persisted to durable storage. |
| 20 | Reply in same detected language | low | allowed | Available | — | No | Detects the input language from text and formats the reply in that same language. | VerificationAgent confirms response matches the normalized result schema. | Held only for the lifetime of the active session/turn; not persisted to durable storage. |
| 21 | Real-time translation mode | low | allowed | Needs external dependency | stt_provider_api_key, tts_provider_api_key | No | Text-to-text translation works today; live spoken translation requires configured STT and TTS providers. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on transient audio/text without creating a durable record. |
| 22 | Emotion/urgency detection | medium | allowed | Needs external dependency | emotion_detection_model | No | Returns external_dependency_required until an emotion-detection model is configured behind the emotion_detector adapter. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on transient audio/text without creating a durable record. |
| 23 | Short driving-mode replies | low | allowed | Available | — | No | Applies a shortened response-formatting profile when driving mode is requested. | VerificationAgent confirms response matches the normalized result schema. | Held only for the lifetime of the active session/turn; not persisted to durable storage. |
| 24 | Bluetooth earbuds routing | medium | allowed | Needs external dependency | bluetooth_audio_worker | No | Returns external_dependency_required until a Bluetooth audio routing worker is configured. | VerificationAgent confirms the reported adapter/config state matches the actual configured integration. | Not persisted; operates on transient audio/text without creating a durable record. |
| 25 | Mobile mic to laptop command routing | medium | allowed | Needs external dependency | mobile_worker_bridge | Yes | Returns external_dependency_required until a cross-device mobile worker bridge is configured. | VerificationAgent confirms the reported adapter/config state matches the actual configured integration. | Held only for the lifetime of the active session/turn; not persisted to durable storage. |
| 26 | Smartwatch mic trigger | low | allowed | Planned | smartwatch_worker | No | No smartwatch worker adapter exists yet; returns a planned/capability_unavailable response. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on transient audio/text without creating a durable record. |
| 27 | Smart glasses mic support | low | allowed | Planned | smart_glasses_worker | No | No smart-glasses worker adapter exists yet; returns a planned/capability_unavailable response. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on transient audio/text without creating a durable record. |
| 28 | Meeting transcription with consent | high | approval_required | Approval required | stt_provider_api_key | Yes | Requires an explicit consent flag before transcription begins; refuses otherwise and returns approval_required. | VerificationAgent confirms a matching audit log row was written for this action. | Persisted only after explicit user/Security Agent approval; sensitive payloads redacted until approved. |
| 29 | Voice note memory with permission | high | approval_required | Approval required | — | Yes | Requires an explicit permission flag on the request; refuses to store without it. | VerificationAgent confirms a matching audit log row was written for this action. | Persisted only after explicit user/Security Agent approval; sensitive payloads redacted until approved. |
| 30 | Voice macro creation | medium | allowed | Available | — | Yes | Stores the macro's command sequence in the DB-backed memory table; does not execute it at creation time. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 31 | Voice-controlled workflows | medium | allowed | Available | — | Yes | Routes the transcribed/typed command to MasterAgent, which dispatches it to the Workflow Agent. | VerificationAgent confirms response matches the normalized result schema. | Held only for the lifetime of the active session/turn; not persisted to durable storage. |
| 32 | Voice-controlled coding | medium | allowed | Available | — | Yes | Routes the transcribed/typed command to MasterAgent, which dispatches it to the Code Agent. | VerificationAgent confirms response matches the normalized result schema. | Held only for the lifetime of the active session/turn; not persisted to durable storage. |
| 33 | Voice-controlled CRM | medium | allowed | Available | — | Yes | Routes the transcribed/typed command to MasterAgent, which dispatches it to the Business Agent. | VerificationAgent confirms response matches the normalized result schema. | Held only for the lifetime of the active session/turn; not persisted to durable storage. |
| 34 | Private mode for sensitive replies | medium | allowed | Available | — | Yes | Applies a redaction formatter to the reply and skips loud TTS playback when private mode is active. | VerificationAgent confirms a matching audit log row was written for this action. | Not persisted; operates on transient audio/text without creating a durable record. |
| 35 | Owner voice recognition before private tasks | high | allowed | Needs external dependency | speaker_verification_model | Yes | Blocks private tasks by default until a speaker-verification model is configured; no task runs on an unverified voice. | VerificationAgent confirms a matching audit log row was written for this action. | Not persisted; operates on transient audio/text without creating a durable record. |
| 36 | Multi-user speaker profiles | medium | allowed | Needs external dependency | speaker_verification_model | Yes | Returns external_dependency_required until a speaker-verification model is configured; profile metadata storage works today. | VerificationAgent confirms the reported adapter/config state matches the actual configured integration. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 37 | Unknown voice blocking for sensitive actions | high | blocked_by_default | Available | — | Yes | Blocks sensitive actions by default whenever speaker identity cannot be confirmed, since no verification model is configured. | VerificationAgent confirms a matching audit log row was written for this action. | Not persisted; operates on transient audio/text without creating a durable record. |
| 38 | Voice command confirmation for risky actions | high | approval_required | Available | — | Yes | Prompts for an explicit yes/no confirmation before forwarding a risky action to the target agent. | VerificationAgent confirms a matching audit log row was written for this action. | Held only for the lifetime of the active session/turn; not persisted to durable storage. |
| 39 | Voice PIN fallback | high | allowed | Available | — | Yes | Checks the entered PIN against a hashed value stored in the DB before allowing a sensitive action to proceed. | VerificationAgent confirms a matching audit log row was written for this action. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 40 | Conversation session timeout | low | allowed | Available | — | No | Tracks session inactivity and closes the session state once the configured timeout elapses. | VerificationAgent confirms response matches the normalized result schema. | Held only for the lifetime of the active session/turn; not persisted to durable storage. |
| 41 | Continuous conversation mode after wake word | medium | allowed | Needs external dependency | wake_word_engine, stt_provider_api_key | No | Returns external_dependency_required until a wake-word engine and STT provider are both configured. | VerificationAgent confirms the reported adapter/config state matches the actual configured integration. | Held only for the lifetime of the active session/turn; not persisted to durable storage. |
| 42 | Context-aware tone selection | low | allowed | Available | — | No | Selects a tone profile from stored preferences and recent conversation context. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 43 | Voice persona profile | low | allowed | Available | — | No | Stores/reads the persona profile via the DB-backed memory table. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 44 | Volume/speed preference memory | low | allowed | Available | — | No | Stores/reads volume and speed preferences via the DB-backed memory table. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 45 | Multi-device audio session manager | medium | allowed | Needs external dependency | multi_device_session_bridge | Yes | Returns external_dependency_required until a multi-device session bridge is configured. | VerificationAgent confirms the reported adapter/config state matches the actual configured integration. | Held only for the lifetime of the active session/turn; not persisted to durable storage. |
| 46 | Voice command history by workspace | low | allowed | Available | — | No | Runs a scoped DB query against the command-history table filtered by workspace_id. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 47 | Audio privacy redaction metadata | medium | allowed | Available | — | Yes | Attaches a redaction-status field to the persisted record instead of storing raw sensitive audio. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 48 | Voice activation audit logs | medium | allowed | Available | — | Yes | Writes an audit log row each time voice activation is triggered. | VerificationAgent confirms a matching audit log row was written for this action. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 49 | TTS proof response for completed task | low | allowed | Needs external dependency | tts_provider_api_key | No | Returns a text confirmation now; spoken confirmation requires a configured TTS provider. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on transient audio/text without creating a durable record. |
| 50 | Voice Agent health/dependency check | low | allowed | Available | — | No | Reports process health plus whether STT, TTS, wake-word, and audio-routing adapters are configured. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on transient audio/text without creating a durable record. |

## System Agent (`system`)

**Purpose:** Device/app/file/OS control with strict approval and safe local workers.

**Example test prompt:** _Check my CPU, RAM, and disk usage right now._

| # | Name | Risk | Permission | Status | Required integrations | Audit | Safe MVP behavior | Verification method | Memory policy |
|---|---|---|---|---|---|---|---|---|---|
| 1 | System health summary | low | allowed | Available | — | No | Computes a live summary from local OS status APIs. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns a live status/read result without creating a durable record. |
| 2 | CPU/RAM/disk check | low | allowed | Available | — | No | Reads live utilization figures from local OS status APIs. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns a live status/read result without creating a durable record. |
| 3 | Battery status | low | allowed | Available | — | No | Reads live battery state from local OS status APIs; reports not-applicable on desktops with no battery. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns a live status/read result without creating a durable record. |
| 4 | Network status | low | allowed | Available | — | No | Reads live connectivity state from local OS network APIs. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns a live status/read result without creating a durable record. |
| 5 | Open app with approval | medium | approval_required | Needs external dependency | windows_worker_agent, mac_worker_agent | Yes | Returns approval_required and, once approved, requires a configured OS worker bridge to actually launch the app. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Persisted only after explicit user/Security Agent approval; the request is logged whether approved or refused. |
| 6 | Close app with approval | medium | approval_required | Needs external dependency | windows_worker_agent, mac_worker_agent | Yes | Returns approval_required and, once approved, requires a configured OS worker bridge to actually close the app. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Persisted only after explicit user/Security Agent approval; the request is logged whether approved or refused. |
| 7 | Focus app/window | low | allowed | Needs external dependency | windows_worker_agent, mac_worker_agent | Yes | Requires a configured OS worker bridge to change window focus; returns external_dependency_required until then. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Held only for the lifetime of the active request; not persisted to durable storage. |
| 8 | Detect frozen app | low | allowed | Needs external dependency | windows_worker_agent, mac_worker_agent | No | Requires a configured OS worker bridge to query process responsiveness; returns external_dependency_required until then. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Not persisted; returns a live status/read result without creating a durable record. |
| 9 | Restart frozen app with approval | high | approval_required | Needs external dependency | windows_worker_agent, mac_worker_agent | Yes | Returns approval_required and, once approved, requires a configured OS worker bridge to kill and relaunch the app. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Persisted only after explicit user/Security Agent approval; the request is logged whether approved or refused. |
| 10 | List allowed directory files | low | allowed | Available | — | No | Lists directory contents using local filesystem access scoped to the allow-listed path only. | VerificationAgent confirms the target file/folder exists (or no longer exists) on disk with the expected state. | Not persisted; returns a live status/read result without creating a durable record. |
| 11 | Create file in allowed directory | medium | allowed | Available | — | Yes | Writes the new file to the allow-listed path using local filesystem access. | VerificationAgent confirms the target file/folder exists (or no longer exists) on disk with the expected state. | Not persisted; returns a live status/read result without creating a durable record. |
| 12 | Rename file | medium | allowed | Available | — | Yes | Renames the file in place using local filesystem access scoped to the allow-listed path. | VerificationAgent confirms the target file/folder exists (or no longer exists) on disk with the expected state. | Not persisted; returns a live status/read result without creating a durable record. |
| 13 | Move file | medium | allowed | Available | — | Yes | Moves the file using local filesystem access scoped to allow-listed paths. | VerificationAgent confirms the target file/folder exists (or no longer exists) on disk with the expected state. | Not persisted; returns a live status/read result without creating a durable record. |
| 14 | Copy file | low | allowed | Available | — | Yes | Copies the file using local filesystem access scoped to allow-listed paths. | VerificationAgent confirms the target file/folder exists (or no longer exists) on disk with the expected state. | Not persisted; returns a live status/read result without creating a durable record. |
| 15 | Delete file only with backup and approval | high | approval_required | Approval required | — | Yes | Creates a backup copy and requests explicit approval before deleting; refuses deletion without both. | VerificationAgent confirms the target file/folder exists (or no longer exists) on disk with the expected state. | Persisted only after explicit user/Security Agent approval; the request is logged whether approved or refused. |
| 16 | Compress folder | low | allowed | Available | — | Yes | Creates a zip archive of the folder using local filesystem access. | VerificationAgent confirms the target file/folder exists (or no longer exists) on disk with the expected state. | Not persisted; returns a live status/read result without creating a durable record. |
| 17 | Organize downloads | medium | allowed | Available | — | Yes | Moves files into type-based subfolders within the allow-listed downloads directory. | VerificationAgent confirms the target file/folder exists (or no longer exists) on disk with the expected state. | Not persisted; returns a live status/read result without creating a durable record. |
| 18 | Protected folder rules | medium | allowed | Available | — | Yes | Checks every file operation's target path against a protected-folder rule set before executing. | VerificationAgent confirms a matching audit log row was written for this action. | Stored in the DB-backed memory/audit tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 19 | Safe OS command preview | low | allowed | Available | — | No | Parses and describes the command's effect using a local safe-command analyzer; never executes it. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns a live status/read result without creating a durable record. |
| 20 | Run approved safe OS command | high | approval_required | Needs external dependency | windows_worker_agent, mac_worker_agent | Yes | Returns approval_required and, once approved, requires a configured OS worker bridge to execute the command on the user's device. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Persisted only after explicit user/Security Agent approval; the request is logged whether approved or refused. |
| 21 | Block dangerous terminal commands | low | blocked_by_default | Available | — | Yes | Checks every command against a local dangerous-command blocklist and refuses execution on a match. | VerificationAgent confirms a matching audit log row was written for this action. | Not persisted; returns a live status/read result without creating a durable record. |
| 22 | Port/service status check | low | allowed | Available | — | No | Checks local port/service state using standard OS networking APIs. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns a live status/read result without creating a durable record. |
| 23 | Start safe local service with approval | high | approval_required | Needs external dependency | windows_worker_agent, mac_worker_agent | Yes | Returns approval_required and, once approved, requires a configured OS worker bridge to start the service. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Persisted only after explicit user/Security Agent approval; the request is logged whether approved or refused. |
| 24 | Stop service with approval | high | approval_required | Needs external dependency | windows_worker_agent, mac_worker_agent | Yes | Returns approval_required and, once approved, requires a configured OS worker bridge to stop the service. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Persisted only after explicit user/Security Agent approval; the request is logged whether approved or refused. |
| 25 | Control volume adapter | low | allowed | Needs external dependency | windows_worker_agent, mac_worker_agent | Yes | Requires a configured OS worker bridge to change system volume; returns external_dependency_required until then. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Held only for the lifetime of the active request; not persisted to durable storage. |
| 26 | Control brightness adapter | low | allowed | Needs external dependency | windows_worker_agent, mac_worker_agent | Yes | Requires a configured OS worker bridge to change display brightness; returns external_dependency_required until then. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Held only for the lifetime of the active request; not persisted to durable storage. |
| 27 | WiFi status adapter | low | allowed | Needs external dependency | windows_worker_agent, mac_worker_agent | No | Requires a configured OS worker bridge to read WiFi adapter state; returns external_dependency_required until then. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Not persisted; returns a live status/read result without creating a durable record. |
| 28 | Bluetooth status adapter | low | allowed | Needs external dependency | windows_worker_agent, mac_worker_agent | No | Requires a configured OS worker bridge to read Bluetooth adapter state; returns external_dependency_required until then. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Not persisted; returns a live status/read result without creating a durable record. |
| 29 | Lock screen with approval | medium | approval_required | Needs external dependency | windows_worker_agent, mac_worker_agent | Yes | Returns approval_required and, once approved, requires a configured OS worker bridge to lock the screen. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Persisted only after explicit user/Security Agent approval; the request is logged whether approved or refused. |
| 30 | Sleep/restart/shutdown requires approval | high | approval_required | Needs external dependency | windows_worker_agent, mac_worker_agent | Yes | Returns approval_required and, once approved, requires a configured OS worker bridge to perform the power action. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Persisted only after explicit user/Security Agent approval; the request is logged whether approved or refused. |
| 31 | Keyboard shortcut automation | high | allowed | Needs external dependency | windows_worker_agent, mac_worker_agent | Yes | Requires a configured OS worker bridge to inject the keystroke; returns external_dependency_required until then. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Held only for the lifetime of the active request; not persisted to durable storage. |
| 32 | Typing automation with approval | high | approval_required | Needs external dependency | windows_worker_agent, mac_worker_agent | Yes | Returns approval_required and, once approved, requires a configured OS worker bridge to inject keystrokes. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Persisted only after explicit user/Security Agent approval; the request is logged whether approved or refused. |
| 33 | Mouse click automation with approval | high | approval_required | Needs external dependency | windows_worker_agent, mac_worker_agent | Yes | Returns approval_required and, once approved, requires a configured OS worker bridge to inject mouse events. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Persisted only after explicit user/Security Agent approval; the request is logged whether approved or refused. |
| 34 | Scroll/drag automation with approval | high | approval_required | Needs external dependency | windows_worker_agent, mac_worker_agent | Yes | Returns approval_required and, once approved, requires a configured OS worker bridge to inject scroll/drag events. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Persisted only after explicit user/Security Agent approval; the request is logged whether approved or refused. |
| 35 | Macro recorder | medium | allowed | Available | — | Yes | Stores the recorded action sequence as data in the DB-backed memory table; does not execute anything while recording. | VerificationAgent confirms a matching audit log row was written for this action. | Stored in the DB-backed memory/audit tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 36 | Macro replay with approval | high | approval_required | Needs external dependency | windows_worker_agent, mac_worker_agent | Yes | Returns approval_required and, once approved, requires a configured OS worker bridge to replay each recorded action. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Persisted only after explicit user/Security Agent approval; the request is logged whether approved or refused. |
| 37 | Screen-aware automation handoff to Visual Agent | medium | allowed | Available | — | Yes | Packages the automation request and routes it to the Visual Agent for screen analysis before any action runs. | VerificationAgent confirms response matches the normalized result schema. | Held only for the lifetime of the active request; not persisted to durable storage. |
| 38 | Notification reading with permission | high | approval_required | Needs external dependency | windows_worker_agent, android_worker_bridge | Yes | Requires explicit permission and a configured OS/mobile worker bridge to read notifications; refuses otherwise. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Persisted only after explicit user/Security Agent approval; the request is logged whether approved or refused. |
| 39 | Draft message reply with approval | medium | approval_required | Available | — | Yes | Generates a draft reply as text for the user to review; never sends it automatically. | VerificationAgent confirms response matches the normalized result schema. | Held only for the lifetime of the active request; not persisted to durable storage. |
| 40 | Call answer/reject with permission | high | approval_required | Needs external dependency | android_worker_bridge, ios_shortcuts_bridge | Yes | Requires explicit permission and a configured mobile worker bridge to answer/reject calls; refuses otherwise. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Persisted only after explicit user/Security Agent approval; the request is logged whether approved or refused. |
| 41 | Windows worker bridge | low | allowed | Needs external dependency | windows_worker_agent | No | Reports connection status; real device actions stay unavailable until the Windows worker node is paired and configured. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Not persisted; returns a live status/read result without creating a durable record. |
| 42 | Mac worker bridge | low | allowed | Needs external dependency | mac_worker_agent | No | Reports connection status; real device actions stay unavailable until the Mac worker node is paired and configured. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Not persisted; returns a live status/read result without creating a durable record. |
| 43 | Android Accessibility worker bridge | low | allowed | Needs external dependency | android_worker_bridge | No | Reports connection status; real device actions stay unavailable until the Android Accessibility worker is paired and configured. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Not persisted; returns a live status/read result without creating a durable record. |
| 44 | iOS Shortcuts bridge | low | allowed | Planned | ios_shortcuts_bridge | No | No iOS worker client is implemented yet (see apps/worker_nodes/ios/ios_client_plan.md); returns a planned response. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; returns a live status/read result without creating a durable record. |
| 45 | Smart device routing | medium | allowed | Needs external dependency | smart_device_hub_integration | Yes | Returns external_dependency_required until a smart-device hub integration is configured. | VerificationAgent confirms the worker bridge reported the device action succeeded before marking the task complete. | Held only for the lifetime of the active request; not persisted to durable storage. |
| 46 | Public/private mode switching | low | allowed | Available | — | Yes | Flips a per-session mode flag that governs how much detail is shown/spoken. | VerificationAgent confirms response matches the normalized result schema. | Held only for the lifetime of the active request; not persisted to durable storage. |
| 47 | Banking app restricted mode | high | allowed | Available | — | Yes | Applies a local rule set that refuses automation targeting apps matched against a banking/finance denylist. | VerificationAgent confirms a matching audit log row was written for this action. | Not persisted; returns a live status/read result without creating a durable record. |
| 48 | File backup before risky operations | medium | allowed | Available | — | Yes | Creates a timestamped local backup copy before the risky file operation proceeds. | VerificationAgent confirms the target file/folder exists (or no longer exists) on disk with the expected state. | Not persisted; returns a live status/read result without creating a durable record. |
| 49 | Action proof handoff to Verification Agent | low | allowed | Available | — | Yes | Packages the action's before/after state and forwards it to the Verification Agent. | VerificationAgent confirms a matching audit log row was written for this action. | Held only for the lifetime of the active request; not persisted to durable storage. |
| 50 | System Agent health/dependency check | low | allowed | Available | — | No | Reports process health plus whether Windows/Mac/Android/iOS worker bridges are configured. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns a live status/read result without creating a durable record. |

## Browser Agent (`browser`)

**Purpose:** Internet research, public page extraction, SEO, competitor analysis, safe browser automation.

**Example test prompt:** _Research our top 3 competitors' pricing pages and summarize their plans._

| # | Name | Risk | Permission | Status | Required integrations | Audit | Safe MVP behavior | Verification method | Memory policy |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Build Google/Bing search plans | low | allowed | Available | — | No | Builds a structured query plan as text/data; no network request is made at this step. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 2 | Country/city/niche search filters | low | allowed | Available | — | No | Applies filter parameters to the structured search plan locally. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 3 | Open public URL adapter | low | allowed | Needs external dependency | playwright_browser_worker | No | Returns external_dependency_required until a Playwright/browser automation worker is configured. | VerificationAgent confirms the browser worker reported the navigation/action succeeded with the expected page state. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 4 | Manage multiple research tabs | low | allowed | Needs external dependency | playwright_browser_worker | No | Requires a configured browser automation worker to maintain a real multi-tab session. | VerificationAgent confirms the browser worker reported the navigation/action succeeded with the expected page state. | Held only for the lifetime of the active research session; not persisted to durable storage. |
| 5 | Purpose-labeled tabs | low | allowed | Needs external dependency | playwright_browser_worker | No | Tags tab metadata once a real multi-tab browser session exists via a configured browser worker. | VerificationAgent confirms the browser worker reported the navigation/action succeeded with the expected page state. | Held only for the lifetime of the active research session; not persisted to durable storage. |
| 6 | Public page extraction | low | allowed | Available | — | No | Parses already-fetched HTML/text content using a local extraction pipeline. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 7 | Extract page title/meta | low | allowed | Available | — | No | Parses title/meta tags from already-fetched HTML using a local extraction pipeline. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 8 | Extract H1/H2 headings | low | allowed | Available | — | No | Parses heading elements from already-fetched HTML using a local extraction pipeline. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 9 | Extract CTA text | low | allowed | Available | — | No | Parses CTA-like elements from already-fetched HTML using a local heuristic extractor. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 10 | Extract pricing tables | low | allowed | Available | — | No | Parses pricing-table structures from already-fetched HTML using a local extraction pipeline. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 11 | Extract FAQs | low | allowed | Available | — | No | Parses FAQ-like structures from already-fetched HTML using a local extraction pipeline. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 12 | Extract testimonials | low | allowed | Available | — | No | Parses testimonial-like structures from already-fetched HTML using a local extraction pipeline. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 13 | Extract contact info | medium | allowed | Available | — | Yes | Parses publicly listed contact details from already-fetched HTML; treats results as PII and redacts in shared summaries. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 14 | SEO title/meta/H1 analysis | low | allowed | Available | — | No | Scores title/meta/H1 content against local SEO heuristics. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 15 | Schema/internal link analysis | low | allowed | Available | — | No | Parses schema.org markup and internal links from already-fetched HTML. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 16 | Broken link detection | low | allowed | Available | — | No | Issues lightweight status checks against each discovered link and reports failures. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 17 | Landing page conversion audit | low | allowed | Available | — | No | Scores extracted page content (CTAs, headings, trust signals) against local conversion heuristics. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 18 | Competitor comparison | low | allowed | Available | — | No | Aggregates extraction results across multiple already-fetched competitor pages. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 19 | Review site research | low | allowed | Available | — | No | Extracts and summarizes publicly available review content already fetched. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 20 | Market/niche research | low | allowed | Available | — | No | Aggregates extraction/analysis results across multiple public sources. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 21 | SaaS feature research | low | allowed | Available | — | No | Extracts and compares feature lists/pricing across already-fetched competitor pages. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 22 | Lead magnet research | low | allowed | Available | — | No | Extracts lead-magnet/opt-in offer content from already-fetched pages. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 23 | Ad copy inspiration research | low | allowed | Available | — | No | Extracts publicly visible ad/promotional copy from already-fetched pages. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 24 | Screenshot capture for reports | low | allowed | Needs external dependency | playwright_browser_worker | No | Returns external_dependency_required until a Playwright/browser automation worker capable of rendering and capturing is configured. | VerificationAgent confirms the browser worker reported the navigation/action succeeded with the expected page state. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 25 | Download public reports | medium | allowed | Available | — | Yes | Downloads the public file to local scoped storage via a standard HTTP request. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 26 | Public data extraction to CSV | low | allowed | Available | — | Yes | Writes the extracted data to a CSV file in scoped local storage. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 27 | Public data extraction to JSON | low | allowed | Available | — | Yes | Writes the extracted data to a JSON file in scoped local storage. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 28 | Source credibility ranking | low | allowed | Available | — | No | Scores sources against a local heuristic (domain age signals, HTTPS, citation presence). | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 29 | Multi-source comparison | low | allowed | Available | — | No | Aggregates and diffs extraction results across already-fetched sources. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 30 | Browser session memory | low | allowed | Available | — | No | Stores/reads session state via the DB-backed memory table. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 31 | Website change monitoring | medium | allowed | Available | — | Yes | Periodically re-fetches the page and diffs it against the last stored snapshot. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 32 | Pricing monitoring alerts | medium | allowed | Available | — | Yes | Diffs extracted pricing-table data against the last stored snapshot and raises an alert on change. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 33 | Workflow learning from websites | medium | allowed | Available | — | Yes | Extracts a step sequence from already-fetched page content and forwards it to the Workflow Agent as a draft. | VerificationAgent confirms response matches the normalized result schema. | Held only for the lifetime of the active research session; not persisted to durable storage. |
| 34 | Form detection | low | allowed | Available | — | No | Parses form elements from already-fetched HTML using a local extraction pipeline. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 35 | Form fill with approval only | high | approval_required | Needs external dependency | playwright_browser_worker | Yes | Returns approval_required and, once approved, requires a configured browser automation worker to fill and submit the form. | VerificationAgent confirms the browser worker reported the navigation/action succeeded with the expected page state. | Persisted/executed only after explicit user/Security Agent approval; the request is logged either way. |
| 36 | Never bypass CAPTCHA | high | blocked_by_default | Available | — | Yes | Detects CAPTCHA challenges and always refuses to bypass or automate past them. | VerificationAgent confirms a matching audit log row was written for this action. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 37 | Never bypass paywalls | high | blocked_by_default | Available | — | Yes | Detects paywalled content and always refuses to bypass or circumvent access restrictions. | VerificationAgent confirms a matching audit log row was written for this action. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 38 | Login-required site detection | low | allowed | Available | — | No | Detects login-wall indicators in already-fetched HTML and reports the page as inaccessible without credentials. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 39 | Browser safe-mode permissions | low | allowed | Available | — | No | Applies the workspace's safe-mode permission policy to every browser action request. | VerificationAgent confirms a matching audit log row was written for this action. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 40 | Cookie/session privacy policy | low | allowed | Available | — | No | Discards cookies/session state at the end of each research session rather than persisting them. | VerificationAgent confirms a matching audit log row was written for this action. | Held only for the lifetime of the active research session; not persisted to durable storage. |
| 41 | URL safety check with Security Agent | medium | allowed | Available | — | Yes | Checks the URL against a local reputation/blocklist heuristic and routes ambiguous cases to the Security Agent. | VerificationAgent confirms a matching audit log row was written for this action. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 42 | Phishing page detection handoff | medium | allowed | Available | — | Yes | Flags pages matching phishing heuristics and forwards them to the Security Agent. | VerificationAgent confirms a matching audit log row was written for this action. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 43 | Competitor content gap report | low | allowed | Available | — | No | Diffs extracted topic/heading coverage between the user's site and competitor sites. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 44 | Local SEO audit | low | allowed | Available | — | No | Scores extracted contact/schema data against local-SEO heuristics. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 45 | Page speed signal placeholder | low | allowed | Needs external dependency | pagespeed_insights_api_key | No | Returns external_dependency_required until a page-speed measurement API is configured; no fabricated score is returned. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 46 | Accessibility quick scan | low | allowed | Available | — | No | Runs local heuristic checks (missing alt text, aria attributes) against already-fetched HTML. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 47 | Screenshot proof handoff | low | allowed | Needs external dependency | playwright_browser_worker | Yes | Depends on screenshot capture; returns external_dependency_required until a browser automation worker is configured. | VerificationAgent confirms the browser worker reported the navigation/action succeeded with the expected page state. | Held only for the lifetime of the active research session; not persisted to durable storage. |
| 48 | Research summary memory save | low | allowed | Available | — | No | Stores the summary via the DB-backed memory table. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 49 | Browser Agent health/dependency check | low | allowed | Available | — | No | Reports process health plus whether the Playwright/browser automation worker is configured. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-fetched public content. |
| 50 | Browser action audit log | medium | allowed | Available | — | Yes | Writes an audit log row for each browser action performed. | VerificationAgent confirms a matching audit log row was written for this action. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |

## Code Agent (`code`)

**Purpose:** Build, edit, debug, test, and deploy code safely.

**Example test prompt:** _Add input validation to the login form and write a unit test for it._

| # | Name | Risk | Permission | Status | Required integrations | Audit | Safe MVP behavior | Verification method | Memory policy |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Project structure analysis | low | allowed | Available | — | No | Walks the project tree and summarizes structure using local static analysis. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-available project files. |
| 2 | Framework detection | low | allowed | Available | — | No | Inspects config/manifest files locally to identify frameworks in use. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-available project files. |
| 3 | Dependency file detection | low | allowed | Available | — | No | Scans the project tree locally for known dependency manifest filenames. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-available project files. |
| 4 | Generate folder structure plan | low | allowed | Available | — | No | Generates a proposed structure as text/data without creating any files. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-available project files. |
| 5 | Generate full final files | medium | allowed | Available | — | Yes | Writes the generated file(s) to disk in the project workspace. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Not persisted; returns an analysis result derived from already-available project files. |
| 6 | Patch existing files safely | medium | allowed | Available | — | Yes | Applies the patch to the target file on disk and preserves surrounding content. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Not persisted; returns an analysis result derived from already-available project files. |
| 7 | Exact block replacement mode | medium | allowed | Available | — | Yes | Locates the exact matching block and replaces only that block on disk. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Not persisted; returns an analysis result derived from already-available project files. |
| 8 | Preserve class/function names | low | allowed | Available | — | No | Diffs symbol names before/after a proposed patch and flags unintended renames. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-available project files. |
| 9 | Read logs and tracebacks | low | allowed | Available | — | No | Reads the supplied log/traceback text and parses it into a structured error summary. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-available project files. |
| 10 | Root-cause analysis | low | allowed | Available | — | No | Cross-references the parsed traceback with the relevant source files to propose a root cause. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-available project files. |
| 11 | Self-debug loop with max attempts | medium | allowed | Available | — | Yes | Applies successive local patches up to a configured attempt limit, stopping and reporting if unresolved. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Held only for the lifetime of the active task; not persisted to durable storage. |
| 12 | Run syntax checks with approval | low | approval_required | Approval required | — | Yes | Returns approval_required; once approved, runs a local syntax/compile check (no network access) and reports the result. | VerificationAgent confirms the command's exit code and captured output match the expected outcome before marking success. | Executed/persisted only after explicit user/Security Agent approval; the request is logged either way. |
| 13 | Run tests with approval | medium | approval_required | Approval required | — | Yes | Returns approval_required; once approved, runs the local test suite (e.g. pytest) and reports pass/fail results. | VerificationAgent confirms the command's exit code and captured output match the expected outcome before marking success. | Executed/persisted only after explicit user/Security Agent approval; the request is logged either way. |
| 14 | Run frontend build with approval | medium | approval_required | Approval required | — | Yes | Returns approval_required; once approved, runs the local frontend build command and reports the result. | VerificationAgent confirms the command's exit code and captured output match the expected outcome before marking success. | Executed/persisted only after explicit user/Security Agent approval; the request is logged either way. |
| 15 | Run backend smoke tests | medium | approval_required | Approval required | — | Yes | Returns approval_required; once approved, runs the local API-smoke test path and reports the result. | VerificationAgent confirms the command's exit code and captured output match the expected outcome before marking success. | Executed/persisted only after explicit user/Security Agent approval; the request is logged either way. |
| 16 | Generate unit tests | low | allowed | Available | — | Yes | Writes generated unit test file(s) to disk; does not execute them. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Not persisted; returns an analysis result derived from already-available project files. |
| 17 | Generate API tests | low | allowed | Available | — | Yes | Writes generated API test file(s) to disk; does not execute them. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Not persisted; returns an analysis result derived from already-available project files. |
| 18 | Generate integration tests | low | allowed | Available | — | Yes | Writes generated integration test file(s) to disk; does not execute them. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Not persisted; returns an analysis result derived from already-available project files. |
| 19 | Generate docs/README | low | allowed | Available | — | Yes | Writes the generated documentation file to disk. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Not persisted; returns an analysis result derived from already-available project files. |
| 20 | Generate Docker files | low | allowed | Available | — | Yes | Writes the generated Docker configuration file(s) to disk; does not build or run any image. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Not persisted; returns an analysis result derived from already-available project files. |
| 21 | Generate CI/CD workflows | low | allowed | Available | — | Yes | Writes the generated CI/CD workflow file to disk; does not trigger any pipeline run. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Not persisted; returns an analysis result derived from already-available project files. |
| 22 | Git status with approval | low | approval_required | Approval required | — | Yes | Returns approval_required; once approved, runs `git status` locally and reports the result. Read-only, no mutation. | VerificationAgent confirms the git working tree/history reflects the expected change via `git status`/`git log`. | Executed/persisted only after explicit user/Security Agent approval; the request is logged either way. |
| 23 | Git diff summary | low | allowed | Available | — | No | Runs `git diff` locally (read-only) and summarizes the changes. | VerificationAgent confirms the git working tree/history reflects the expected change via `git status`/`git log`. | Not persisted; returns an analysis result derived from already-available project files. |
| 24 | Git commit only with explicit approval | high | approval_required | Approval required | — | Yes | Refuses to commit unless the user has given an explicit commit instruction; commits locally once approved. | VerificationAgent confirms the git working tree/history reflects the expected change via `git status`/`git log`. | Executed/persisted only after explicit user/Security Agent approval; the request is logged either way. |
| 25 | Rollback unsafe patches | medium | allowed | Available | — | Yes | Reverts the target file(s) to their pre-patch state on disk. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Not persisted; returns an analysis result derived from already-available project files. |
| 26 | Database model builder | low | allowed | Available | — | Yes | Writes the generated model file to disk following the project's existing model conventions. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Not persisted; returns an analysis result derived from already-available project files. |
| 27 | Migration generator | low | allowed | Available | — | Yes | Writes the generated migration file to disk; does not apply it to any database. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Not persisted; returns an analysis result derived from already-available project files. |
| 28 | REST API builder | low | allowed | Available | — | Yes | Writes the generated endpoint code to disk following existing route conventions. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Not persisted; returns an analysis result derived from already-available project files. |
| 29 | FastAPI router builder | low | allowed | Available | — | Yes | Writes the generated router file to disk following the project's router conventions. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Not persisted; returns an analysis result derived from already-available project files. |
| 30 | React/Next page builder | low | allowed | Available | — | Yes | Writes the generated page/component file to disk under apps/dashboard. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Not persisted; returns an analysis result derived from already-available project files. |
| 31 | Flutter screen builder | low | allowed | Available | — | Yes | Writes the generated Flutter screen file to disk following the project's conventions. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Not persisted; returns an analysis result derived from already-available project files. |
| 32 | Android Kotlin module builder | low | allowed | Available | — | Yes | Writes the generated Kotlin module file(s) to disk under apps/worker_nodes/android. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Not persisted; returns an analysis result derived from already-available project files. |
| 33 | Package/dependency manager | high | approval_required | Approval required | — | Yes | Returns approval_required; once approved, runs the local package manager (pip/npm) command and reports the result. | VerificationAgent confirms the command's exit code and captured output match the expected outcome before marking success. | Executed/persisted only after explicit user/Security Agent approval; the request is logged either way. |
| 34 | Security scan for secrets | medium | allowed | Available | — | Yes | Runs a local regex/pattern scan for secret-like strings across the project tree. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-available project files. |
| 35 | Injection risk scan | medium | allowed | Available | — | No | Runs a local static-analysis pattern scan for unsafe query/command construction. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-available project files. |
| 36 | User/workspace isolation checker | medium | allowed | Available | — | No | Runs a local static-analysis pass looking for DB/queries missing user_id/workspace_id filters. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-available project files. |
| 37 | Endpoint health checker | low | allowed | Available | — | No | Issues local health-check requests against configured endpoints and reports status. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-available project files. |
| 38 | Deployment assistant | high | approval_required | Needs external dependency | deployment_target_credentials | Yes | Returns approval_required and external_dependency_required until deployment target credentials are configured; no deploy runs without both. | VerificationAgent confirms the command's exit code and captured output match the expected outcome before marking success. | Executed/persisted only after explicit user/Security Agent approval; the request is logged either way. |
| 39 | APK build assistant | medium | approval_required | Needs external dependency | android_build_toolchain | Yes | Returns approval_required and external_dependency_required until an Android build toolchain/SDK is configured. | VerificationAgent confirms the command's exit code and captured output match the expected outcome before marking success. | Executed/persisted only after explicit user/Security Agent approval; the request is logged either way. |
| 40 | Code style normalization | low | allowed | Available | — | Yes | Runs the project's local formatter against changed files and writes the formatted result to disk. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Not persisted; returns an analysis result derived from already-available project files. |
| 41 | Compatibility stubs for missing files | low | allowed | Available | — | Yes | Writes a stub module to disk following this repo's import-safe try/except fallback pattern. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Not persisted; returns an analysis result derived from already-available project files. |
| 42 | Environment config validator | low | allowed | Available | — | No | Compares .env against .env.example locally and reports missing/extra keys. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-available project files. |
| 43 | TypeScript error fixer | medium | allowed | Available | — | Yes | Parses tsc error output and applies a targeted patch to the offending file(s) on disk. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Not persisted; returns an analysis result derived from already-available project files. |
| 44 | Python import graph checker | low | allowed | Available | — | No | Statically parses import statements across the project tree to build the graph. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-available project files. |
| 45 | Circular import detector | low | allowed | Available | — | No | Analyzes the local import graph for cycles and reports the offending chain. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-available project files. |
| 46 | Test coverage summary | low | allowed | Available | — | No | Parses an already-generated coverage report and summarizes it; does not run tests itself. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-available project files. |
| 47 | Changelog writer | low | allowed | Available | — | Yes | Reads local git log output and writes a generated changelog file to disk. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Not persisted; returns an analysis result derived from already-available project files. |
| 48 | Architecture report writer | low | allowed | Available | — | Yes | Analyzes the project tree/imports and writes a generated architecture report file to disk. | VerificationAgent confirms the target file(s) exist on disk with the expected updated content/mtime. | Not persisted; returns an analysis result derived from already-available project files. |
| 49 | Code memory save | low | allowed | Available | — | No | Stores the note via the DB-backed memory table with a code-convention tag. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 50 | Code Agent health/dependency check | low | allowed | Available | — | No | Reports process health plus whether local test/build tooling and deployment credentials are configured. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; returns an analysis result derived from already-available project files. |

## Memory Agent (`memory`)

**Purpose:** Short/long/project/client/team memory with privacy, recall, and workspace isolation.

**Example test prompt:** _Remember that our client Acme Corp prefers email over calls._

| # | Name | Risk | Permission | Status | Required integrations | Audit | Safe MVP behavior | Verification method | Memory policy |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Short-term session memory | low | allowed | Available | — | No | Store in an in-process/session-scoped cache keyed by session_id. | VerificationAgent confirms response matches the normalized result schema. | Held only for the lifetime of the active session; not persisted to durable storage. |
| 2 | Long-term preference memory | low | allowed | Available | — | Yes | Store/read via the real DB-backed memory table. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 3 | Project memory | low | allowed | Available | — | Yes | Store/read via the DB-backed memory table with a project tag. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 4 | Client memory | medium | allowed | Available | — | Yes | Store/read via the DB-backed memory table with a client tag. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 5 | Team memory | low | allowed | Available | — | Yes | Store/read via the DB-backed memory table with a team-visibility flag. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 6 | Workspace memory isolation | high | allowed | Available | — | Yes | Every query is filtered by workspace_id at the DB layer; cross-workspace reads are rejected. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 7 | User memory isolation | high | allowed | Available | — | Yes | Every query is filtered by user_id at the DB layer; cross-user reads are rejected. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 8 | Role-based memory access | medium | allowed | Available | — | Yes | Check the caller's role against a category ACL before returning results. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 9 | Keyword recall | low | allowed | Available | — | No | Run a scoped LIKE/full-text query against the memory table. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored memory rather than creating new records. |
| 10 | Semantic recall adapter | low | allowed | Needs external dependency | embeddings_provider_api_key | No | Falls back to keyword recall until an embeddings provider is configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on already-stored memory rather than creating new records. |
| 11 | Vector embeddings adapter | low | allowed | Needs external dependency | vector_db_url, embeddings_provider_api_key | No | Returns capability_unavailable-style response until a vector store/provider is configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on already-stored memory rather than creating new records. |
| 12 | Memory summarization | low | allowed | Available | — | No | Summarize scoped records already in the DB using local text summarization. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored memory rather than creating new records. |
| 13 | Memory deduplication | medium | allowed | Available | — | Yes | Flag likely duplicates by similarity heuristic; merge only after confirmation. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 14 | Memory cleanup | medium | approval_required | Approval required | — | Yes | Proposes a cleanup plan; deletion requires explicit approval. | VerificationAgent confirms a matching audit log row was written for this action. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 15 | Conflict detection | low | allowed | Available | — | No | Compare scoped records and flag contradictions for review. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored memory rather than creating new records. |
| 16 | Knowledge graph nodes | low | allowed | Planned | graph_db_url | No | No graph store exists yet; returns a planned/capability_unavailable response. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on already-stored memory rather than creating new records. |
| 17 | Knowledge graph edges | low | allowed | Planned | graph_db_url | No | No graph store exists yet; returns a planned/capability_unavailable response. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on already-stored memory rather than creating new records. |
| 18 | Project timeline tracking | low | allowed | Available | — | No | Derive a timeline from timestamped project-tagged memory records. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 19 | Decision history | low | allowed | Available | — | Yes | Derive a decision log from tagged memory records. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 20 | Agent-specific memory | low | allowed | Available | — | No | Store/read via the DB-backed memory table with an agent tag. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 21 | Prompt preference recall | low | allowed | Available | — | No | Read from long-term preference memory. | VerificationAgent confirms response matches the normalized result schema. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 22 | Style rule recall | low | allowed | Available | — | No | Read from long-term preference memory. | VerificationAgent confirms response matches the normalized result schema. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 23 | Codebase rule recall | low | allowed | Available | — | No | Read from project memory tagged for code conventions. | VerificationAgent confirms response matches the normalized result schema. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 24 | Client follow-up memory | medium | allowed | Available | — | Yes | Store/read via client memory with a follow-up-due flag. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 25 | CRM memory linking | medium | allowed | Available | — | Yes | Store a foreign-key reference to the CRM record alongside the memory row. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 26 | Voice note memory with consent | high | approval_required | Approval required | — | Yes | Requires an explicit consent flag on the request; refuses to store without it. | VerificationAgent confirms a matching audit log row was written for this action. | Persisted only after explicit user/Security Agent approval; sensitive payloads redacted until approved. |
| 27 | Document memory with consent | high | approval_required | Approval required | — | Yes | Requires an explicit consent flag on the request; refuses to store without it. | VerificationAgent confirms a matching audit log row was written for this action. | Persisted only after explicit user/Security Agent approval; sensitive payloads redacted until approved. |
| 28 | Chat memory with consent | high | approval_required | Approval required | — | Yes | Requires an explicit consent flag on the request; refuses to store without it. | VerificationAgent confirms a matching audit log row was written for this action. | Persisted only after explicit user/Security Agent approval; sensitive payloads redacted until approved. |
| 29 | Privacy classification | medium | allowed | Available | — | Yes | Apply a rule-based sensitivity classifier before persistence. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored memory rather than creating new records. |
| 30 | Sensitive memory blocker | high | blocked_by_default | Available | — | Yes | Rejects the write and returns approval_required instead of silently storing. | VerificationAgent confirms a matching audit log row was written for this action. | Not persisted; operates on already-stored memory rather than creating new records. |
| 31 | Approval flow for sensitive memory | high | approval_required | Approval required | — | Yes | Creates a Security Agent approval request; write proceeds only on approval. | VerificationAgent confirms a matching audit log row was written for this action. | Persisted only after explicit user/Security Agent approval; sensitive payloads redacted until approved. |
| 32 | Forget memory | medium | approval_required | Approval required | — | Yes | Deletes the scoped row after explicit confirmation; logs the deletion. | VerificationAgent confirms a matching audit log row was written for this action. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 33 | Update memory | low | allowed | Available | — | Yes | Update the scoped row in the memory table. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 34 | Export memory | medium | approval_required | Approval required | — | Yes | Generates an export file only after explicit approval; sensitive fields redacted by default. | VerificationAgent confirms a matching audit log row was written for this action. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 35 | Backup memory | medium | allowed | Available | — | Yes | Writes a scoped snapshot to internal storage. | VerificationAgent confirms a matching audit log row was written for this action. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 36 | Restore memory | high | approval_required | Approval required | — | Yes | Restores from a scoped snapshot only after explicit approval. | VerificationAgent confirms a matching audit log row was written for this action. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 37 | Memory import | medium | approval_required | Approval required | — | Yes | Validates and imports scoped records only after explicit approval. | VerificationAgent confirms a matching audit log row was written for this action. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 38 | Multi-device memory sync | low | allowed | Planned | device_sync_service | No | No device-sync transport exists yet; returns a planned response. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on already-stored memory rather than creating new records. |
| 39 | Conflict resolution | medium | allowed | Available | — | Yes | Applies the caller's chosen resolution to the scoped records. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 40 | Memory health scoring | low | allowed | Available | — | No | Compute a score from scoped record metadata. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored memory rather than creating new records. |
| 41 | Memory retention rules | medium | allowed | Available | — | Yes | Reads/applies a per-workspace retention policy record. | VerificationAgent confirms the expected row exists/changed in the memory table for the scoped user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 42 | Memory access audit | medium | allowed | Available | — | Yes | Reads from the audit log table filtered to memory-category events. | VerificationAgent confirms a matching audit log row was written for this action. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 43 | Memory search by project | low | allowed | Available | — | No | Scoped DB query filtered by project tag. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored memory rather than creating new records. |
| 44 | Memory search by client | low | allowed | Available | — | No | Scoped DB query filtered by client tag. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored memory rather than creating new records. |
| 45 | Memory search by date | low | allowed | Available | — | No | Scoped DB query filtered by created_at/updated_at range. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored memory rather than creating new records. |
| 46 | Memory search by agent | low | allowed | Available | — | No | Scoped DB query filtered by agent tag. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored memory rather than creating new records. |
| 47 | Memory safe result formatting | low | allowed | Available | — | No | Applies the redaction formatter to every outbound result. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored memory rather than creating new records. |
| 48 | Memory-to-MasterAgent context injection | low | allowed | Available | — | No | Returns a scoped context bundle consumed by MasterAgent before planning. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored memory rather than creating new records. |
| 49 | Memory Agent health/dependency check | low | allowed | Available | — | No | Reports DB connectivity and whether optional vector/graph adapters are configured. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored memory rather than creating new records. |
| 50 | Memory dashboard metrics | low | allowed | Available | — | No | Computes counts/aggregates from scoped memory records. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored memory rather than creating new records. |

## Security Agent (`security`)

**Purpose:** Risk, approval, policy, audit, fraud, anomaly, and permission brain.

**Example test prompt:** _Show me all pending approval requests from the last 24 hours._

| # | Name | Risk | Permission | Status | Required integrations | Audit | Safe MVP behavior | Verification method | Memory policy |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Action classification | low | allowed | Available | — | No | Applies a rule-based classifier (action type/keyword/category match) and returns the classification synchronously. | VerificationAgent confirms the response matches the normalized result schema. | Not persisted; produces an in-request classification/decision without creating a new record. |
| 2 | Risk scoring | low | allowed | Available | — | No | Computes a rule-based risk score from action classification, target, and requester context. | VerificationAgent confirms the response matches the normalized result schema. | Not persisted; produces an in-request classification/decision without creating a new record. |
| 3 | Permission checking | medium | allowed | Available | — | Yes | Checks the caller's role/permission flags against the action's required permission before allowing it to proceed. | VerificationAgent confirms a matching audit log row was written for this decision. | Not stored as memory; written only to the audit log table for compliance traceability. |
| 4 | Role access enforcement | medium | allowed | Available | — | Yes | Compares the caller's assigned role against the action's role ACL and rejects mismatches. | VerificationAgent confirms a matching audit log row was written for this decision. | Not stored as memory; written only to the audit log table for compliance traceability. |
| 5 | Plan access enforcement | medium | allowed | Available | — | Yes | Checks the workspace's subscription plan against the action's plan requirement via subscriptions/access_control.py before allowing it. | VerificationAgent confirms a matching audit log row was written for this decision. | Not stored as memory; written only to the audit log table for compliance traceability. |
| 6 | Sensitive action approval | high | approval_required | Approval required | — | Yes | Intercepts sensitive actions and blocks execution until an approval decision is recorded. | VerificationAgent confirms an approval-request record exists with the expected resolved state. | Persisted only as part of an approval-request record; sensitive payload fields redacted until resolved. |
| 7 | Approval request creation | medium | allowed | Available | — | Yes | Writes an approval-request record with action context, requester, and risk score. | VerificationAgent confirms an approval-request record exists with the expected resolved state. | Persisted only as part of an approval-request record; sensitive payload fields redacted until resolved. |
| 8 | Approval decision logging | medium | allowed | Available | — | Yes | Writes the approval decision, approver identity, and timestamp to the approval-request record. | VerificationAgent confirms a matching audit log row was written for this decision. | Not stored as memory; written only to the audit log table for compliance traceability. |
| 9 | Denial logging | medium | allowed | Available | — | Yes | Writes a denial record with action, requester, and reason to the audit log. | VerificationAgent confirms a matching audit log row was written for this decision. | Not stored as memory; written only to the audit log table for compliance traceability. |
| 10 | Audit logs | low | allowed | Available | — | No | Reads from the DB-backed audit log table filtered by scoped user_id/workspace_id. | VerificationAgent confirms the response matches the normalized result schema. | Not persisted; produces an in-request classification/decision without creating a new record. |
| 11 | Biometric approval hook | high | approval_required | Needs external dependency | biometric_hardware_sdk | Yes | Falls back to PIN/voice approval until a biometric hardware SDK is configured; never fakes a biometric match. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Persisted only as part of an approval-request record; sensitive payload fields redacted until resolved. |
| 12 | PIN approval hook | high | approval_required | Available | — | Yes | Compares a submitted PIN against its stored hash and records the approval outcome. | VerificationAgent confirms an approval-request record exists with the expected resolved state. | Persisted only as part of an approval-request record; sensitive payload fields redacted until resolved. |
| 13 | Voice approval hook | high | approval_required | Needs external dependency | voice_biometric_provider | Yes | Falls back to PIN approval until a voice biometric provider is configured; never fakes a voice match. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Persisted only as part of an approval-request record; sensitive payload fields redacted until resolved. |
| 14 | Trusted device check | medium | allowed | Available | — | Yes | Compares the request's device fingerprint against the DB-backed trusted-device list. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for this action. | Persisted to the DB-backed audit/policy tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 15 | Unknown device blocking | high | blocked_by_default | Available | — | Yes | Rejects the action and returns approval_required when the device fingerprint has no trusted-device match. | VerificationAgent confirms a matching audit log row was written for this decision. | Not stored as memory; written only to the audit log table for compliance traceability. |
| 16 | Payment guard | high | approval_required | Available | — | Yes | Intercepts every payment_action and blocks execution until explicit approval is recorded. | VerificationAgent confirms an approval-request record exists with the expected resolved state. | Persisted only as part of an approval-request record; sensitive payload fields redacted until resolved. |
| 17 | Banking app guard | high | approval_required | Available | — | Yes | Flags actions targeting a known banking-app allowlist and requires approval before proceeding. | VerificationAgent confirms an approval-request record exists with the expected resolved state. | Persisted only as part of an approval-request record; sensitive payload fields redacted until resolved. |
| 18 | File delete protection | high | approval_required | Available | — | Yes | Intercepts file_delete actions and blocks execution until explicit approval is recorded. | VerificationAgent confirms an approval-request record exists with the expected resolved state. | Persisted only as part of an approval-request record; sensitive payload fields redacted until resolved. |
| 19 | Protected folder enforcement | high | blocked_by_default | Available | — | Yes | Rejects any action targeting a path on the protected-folder list, regardless of approval status. | VerificationAgent confirms a matching audit log row was written for this decision. | Not stored as memory; written only to the audit log table for compliance traceability. |
| 20 | Fraud detection | high | allowed | Available | — | Yes | Applies rule-based fraud heuristics (amount, recipient novelty, velocity) to flag suspicious actions for approval. | VerificationAgent confirms the response matches the normalized result schema. | Not persisted; produces an in-request classification/decision without creating a new record. |
| 21 | Phishing link detection | medium | allowed | Available | — | No | Applies a rule-based URL/domain heuristic check (lookalike domains, known bad patterns) to flag suspicious links. | VerificationAgent confirms the response matches the normalized result schema. | Not persisted; produces an in-request classification/decision without creating a new record. |
| 22 | Fake invoice detection | medium | allowed | Available | — | Yes | Applies rule-based checks (mismatched bank details, new payee, formatting anomalies) to flag suspicious invoices. | VerificationAgent confirms the response matches the normalized result schema. | Not persisted; produces an in-request classification/decision without creating a new record. |
| 23 | Suspicious process detection | medium | allowed | Available | — | Yes | Applies a rule-based process-name/behavior heuristic to process data supplied by the System Agent. | VerificationAgent confirms the response matches the normalized result schema. | Not persisted; produces an in-request classification/decision without creating a new record. |
| 24 | Suspicious browser extension detection | medium | allowed | Available | — | Yes | Applies a rule-based extension allowlist/permission heuristic to extension data supplied by the Browser Agent. | VerificationAgent confirms the response matches the normalized result schema. | Not persisted; produces an in-request classification/decision without creating a new record. |
| 25 | Threat monitor | medium | allowed | Available | — | Yes | Polls recent scoped audit/event data against a rule-based threat-indicator list. | VerificationAgent confirms the response matches the normalized result schema. | Not persisted; produces an in-request classification/decision without creating a new record. |
| 26 | Anomaly detection | medium | allowed | Available | — | Yes | Applies rule-based thresholds (time-of-day, volume, new target) against scoped historical activity. | VerificationAgent confirms the response matches the normalized result schema. | Not persisted; produces an in-request classification/decision without creating a new record. |
| 27 | Session timeout | low | allowed | Available | — | No | Enforces a configurable inactivity timeout and invalidates the session token when exceeded. | VerificationAgent confirms the response matches the normalized result schema. | Held only for the lifetime of the active session/request; not persisted to durable storage. |
| 28 | Re-authentication requirement | high | approval_required | Available | — | Yes | Blocks the action and prompts for credential re-entry when the session exceeds the re-auth threshold. | VerificationAgent confirms an approval-request record exists with the expected resolved state. | Held only for the lifetime of the active session/request; not persisted to durable storage. |
| 29 | App lock | medium | allowed | Available | — | Yes | Marks the target app as locked and requires successful re-authentication to unlock. | VerificationAgent confirms a matching audit log row was written for this decision. | Persisted to the DB-backed audit/policy tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 30 | Privacy guard | medium | allowed | Available | — | No | Applies a rule-based field-level privacy filter to outbound agent responses. | VerificationAgent confirms the response matches the normalized result schema. | Not persisted; produces an in-request classification/decision without creating a new record. |
| 31 | Secret redaction | medium | allowed | Available | — | No | Applies a regex/pattern-based secret scanner via security/secrets_manager.py before logging or returning content. | VerificationAgent confirms the response matches the normalized result schema. | Not persisted; produces an in-request classification/decision without creating a new record. |
| 32 | Prevent hidden monitoring | high | blocked_by_default | Available | — | Yes | Rejects any monitoring-capable action that lacks explicit, visible user consent, regardless of requester. | VerificationAgent confirms a matching audit log row was written for this decision. | Not stored as memory; written only to the audit log table for compliance traceability. |
| 33 | Prevent auto-transfer | critical | blocked_by_default | Available | — | Yes | Hard-blocks any transfer/payment action lacking an explicit, single-use approval token. | VerificationAgent confirms an approval-request record exists with the expected resolved state. | Not stored as memory; written only to the audit log table for compliance traceability. |
| 34 | Prevent unsafe terminal commands | critical | blocked_by_default | Available | — | Yes | Matches requested commands against a rule-based deny-list (e.g. recursive delete, disk format, permission changes) and blocks matches outright. | VerificationAgent confirms a matching audit log row was written for this decision. | Not stored as memory; written only to the audit log table for compliance traceability. |
| 35 | Emergency lock | critical | allowed | Available | — | Yes | Suspends all agent execution and requires re-authentication for the scoped user/workspace the moment it is invoked. | VerificationAgent confirms a matching audit log row was written for this decision. | Not stored as memory; written only to the audit log table for compliance traceability. |
| 36 | Kill switch | critical | allowed | Available | — | Yes | Signals the task manager to cancel/stop all in-flight tasks for the scoped target. | VerificationAgent confirms a matching audit log row was written for this decision. | Not stored as memory; written only to the audit log table for compliance traceability. |
| 37 | Freeze automation | critical | allowed | Available | — | Yes | Sets a freeze flag that the Workflow Agent checks before executing any scheduled/automated run. | VerificationAgent confirms a matching audit log row was written for this decision. | Persisted to the DB-backed audit/policy tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 38 | Per-agent policy engine | medium | allowed | Available | — | No | Loads and evaluates an agent-specific policy section before allowing that agent's actions. | VerificationAgent confirms the response matches the normalized result schema. | Persisted to the DB-backed audit/policy tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 39 | Default policy JSON loader | low | allowed | Available | — | No | Reads and parses the default policy file at startup/on demand, falling back to conservative built-in defaults if the file is missing. | VerificationAgent confirms the response matches the normalized result schema. | Not persisted; produces an in-request classification/decision without creating a new record. |
| 40 | Workspace-specific policy | medium | allowed | Available | — | Yes | Merges a DB-backed workspace policy record over the default policy for scoped evaluation. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for this action. | Persisted to the DB-backed audit/policy tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 41 | User-specific policy override | medium | approval_required | Approval required | — | Yes | Proposes the override and applies it only after Security Agent/admin approval, to prevent silent privilege escalation. | VerificationAgent confirms an approval-request record exists with the expected resolved state. | Persisted only as part of an approval-request record; sensitive payload fields redacted until resolved. |
| 42 | Risk heatmap dashboard | low | allowed | Available | — | No | Aggregates scoped risk scores from the audit log into counts/buckets for the dashboard. | VerificationAgent confirms the response matches the normalized result schema. | Not persisted; produces an in-request classification/decision without creating a new record. |
| 43 | Compliance/security report | low | allowed | Available | — | No | Compiles scoped audit/approval/policy data into a structured report. | VerificationAgent confirms the response matches the normalized result schema. | Not persisted; produces an in-request classification/decision without creating a new record. |
| 44 | Security event WebSocket | low | allowed | Available | — | No | Publishes scoped security events onto the existing WebSocket channel as they occur. | VerificationAgent confirms the response matches the normalized result schema. | Held only for the lifetime of the active session/request; not persisted to durable storage. |
| 45 | Approval expiry | medium | allowed | Available | — | Yes | Marks pending approval requests as expired/denied once their TTL elapses. | VerificationAgent confirms a matching audit log row was written for this decision. | Not stored as memory; written only to the audit log table for compliance traceability. |
| 46 | Multi-step approval chain | high | approval_required | Available | — | Yes | Tracks an ordered list of required approvers and only permits execution once every step is approved. | VerificationAgent confirms an approval-request record exists with the expected resolved state. | Persisted only as part of an approval-request record; sensitive payload fields redacted until resolved. |
| 47 | Audit export | medium | approval_required | Approval required | — | Yes | Generates a scoped audit export only after explicit approval, with sensitive fields redacted by default. | VerificationAgent confirms a matching audit log row was written for this decision. | Persisted to the DB-backed audit/policy tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 48 | Security Agent health/dependency check | low | allowed | Available | — | No | Reports policy-file load status, DB connectivity, and configured approval-hook adapters. | VerificationAgent confirms the response matches the normalized result schema. | Not persisted; produces an in-request classification/decision without creating a new record. |
| 49 | Security test mode | low | allowed | Available | — | No | Evaluates classification/risk/policy logic against a supplied test payload without writing audit/approval records. | VerificationAgent confirms the response matches the normalized result schema. | Held only for the lifetime of the active session/request; not persisted to durable storage. |
| 50 | Security incident summary | medium | allowed | Available | — | Yes | Compiles scoped audit/approval/anomaly records related to an incident into a structured summary. | VerificationAgent confirms the response matches the normalized result schema. | Not persisted; produces an in-request classification/decision without creating a new record. |

## Verification Agent (`verification`)

**Purpose:** Confirm task results with state checks, command output, screenshots, validation, retries, and proof.

**Example test prompt:** _Verify that the last deployment actually succeeded._

| # | Name | Risk | Permission | Status | Required integrations | Audit | Safe MVP behavior | Verification method | Memory policy |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Verify structured result schema | low | allowed | Available | — | No | Runs a JSON-schema/structural check against the returned dict and flags missing/mistyped fields. | Self-verified: the schema checker's own pass/fail output is what is returned. | Not persisted; produces an in-request check result without creating a durable record. |
| 2 | Verify API endpoint response | low | allowed | Available | — | No | Issues a real HTTP request to the endpoint and checks status code and response schema. | Confirmed directly by the HTTP response received; no separate check needed. | Not persisted; produces an in-request check result without creating a durable record. |
| 3 | Verify server port active | low | allowed | Available | — | No | Attempts a real TCP connection to the port and reports open/closed. | Confirmed directly by the socket connection result. | Not persisted; produces an in-request check result without creating a durable record. |
| 4 | Verify file exists | low | allowed | Available | — | No | Checks the filesystem path with a real os.path.exists/stat call. | Confirmed directly by the filesystem check result. | Not persisted; produces an in-request check result without creating a durable record. |
| 5 | Verify folder exists | low | allowed | Available | — | No | Checks the filesystem path with a real os.path.isdir call. | Confirmed directly by the filesystem check result. | Not persisted; produces an in-request check result without creating a durable record. |
| 6 | Verify file modified | low | allowed | Available | — | No | Compares the file's mtime/content hash before and after the action. | Confirmed directly by comparing the captured before/after mtime or content hash. | Held only for the lifetime of the active check/request; not persisted to durable storage. |
| 7 | Verify backup created | medium | allowed | Available | — | Yes | Checks that a new backup artifact exists and its size/timestamp is plausible. | Confirmed directly by the filesystem/snapshot existence check. | Persisted to the DB-backed memory/audit table, keyed by user_id + workspace_id; never mixed across tenants. |
| 8 | Verify app opened | medium | allowed | Available | — | No | Checks process list / window state supplied by the System Agent for the target app. | Confirmed by cross-checking System Agent process/window state. | Not persisted; produces an in-request check result without creating a durable record. |
| 9 | Verify app closed | medium | allowed | Available | — | No | Checks process list / window state supplied by the System Agent for the target app. | Confirmed by cross-checking System Agent process/window state. | Not persisted; produces an in-request check result without creating a durable record. |
| 10 | Verify app focused | medium | allowed | Available | — | No | Checks the active-window state supplied by the System Agent. | Confirmed by cross-checking System Agent active-window state. | Not persisted; produces an in-request check result without creating a durable record. |
| 11 | Verify browser URL loaded | low | allowed | Available | — | No | Checks the Browser Agent's reported current URL against the expected URL. | Confirmed by cross-checking Browser Agent navigation state. | Not persisted; produces an in-request check result without creating a durable record. |
| 12 | Verify page title | low | allowed | Available | — | No | Checks the Browser Agent's reported page title against the expected value/pattern. | Confirmed by cross-checking Browser Agent page metadata. | Not persisted; produces an in-request check result without creating a durable record. |
| 13 | Verify page content | low | allowed | Available | — | No | Checks the Browser Agent's extracted page text for the expected content/pattern. | Confirmed by cross-checking Browser Agent extracted page content. | Not persisted; produces an in-request check result without creating a durable record. |
| 14 | Verify build passed | medium | allowed | Available | — | Yes | Parses the real build command's exit code and output for error markers. | Confirmed directly by the build process exit code and captured output. | Persisted to the DB-backed memory/audit table, keyed by user_id + workspace_id; never mixed across tenants. |
| 15 | Verify tests passed | medium | allowed | Available | — | Yes | Parses the real test runner's exit code and summary output for pass/fail counts. | Confirmed directly by the test runner exit code and summary output. | Persisted to the DB-backed memory/audit table, keyed by user_id + workspace_id; never mixed across tenants. |
| 16 | Verify command output | medium | allowed | Available | — | No | Runs/receives the real command output and matches it against the expected pattern. | Confirmed directly by the captured stdout/stderr and exit code. | Not persisted; produces an in-request check result without creating a durable record. |
| 17 | Screenshot verification adapter | low | allowed | Needs external dependency | screenshot_capture_engine | No | Returns external_dependency_required until a screenshot capture engine is configured; never fabricates a visual match. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; produces an in-request check result without creating a durable record. |
| 18 | OCR success detection adapter | low | allowed | Needs external dependency | ocr_engine | No | Returns external_dependency_required until an OCR engine is configured; never fabricates recognized text. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; produces an in-request check result without creating a durable record. |
| 19 | UI element verification adapter | low | allowed | Needs external dependency | visual_agent_ui_map | No | Delegates to the Visual Agent's UI map when configured; otherwise returns external_dependency_required. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; produces an in-request check result without creating a durable record. |
| 20 | Error screen detection | medium | allowed | Available | — | No | Matches captured page text/system output against a rule-based error-pattern list. | Confirmed directly by the matched error-pattern result. | Not persisted; produces an in-request check result without creating a durable record. |
| 21 | Multi-step workflow validation | medium | allowed | Available | — | Yes | Checks each step's recorded result against its expected postcondition in sequence. | Confirmed directly by aggregating each step's individual verification result. | Persisted to the DB-backed memory/audit table, keyed by user_id + workspace_id; never mixed across tenants. |
| 22 | Failed step detection | medium | allowed | Available | — | Yes | Diffs expected vs actual step results to isolate the first failing step. | Confirmed directly by the per-step verification results. | Persisted to the DB-backed memory/audit table, keyed by user_id + workspace_id; never mixed across tenants. |
| 23 | Retry safe failed steps | medium | approval_required | Available | — | Yes | Retries only steps tagged non-sensitive/idempotent, up to the configured retry limit. | Confirmed by re-running the step's own verification check after the retry. | Persisted to the DB-backed memory/audit table, keyed by user_id + workspace_id; never mixed across tenants. |
| 24 | Never retry sensitive actions | high | blocked_by_default | Available | — | Yes | Hard-blocks auto-retry for sensitive-flagged steps and surfaces the failure for manual/approved re-run instead. | Confirmed directly by the retry-eligibility check refusing to re-invoke the sensitive step. | Not stored as memory; written only to the audit log table for compliance traceability. |
| 25 | Proof screenshot capture with permission | medium | approval_required | Needs external dependency | screenshot_capture_engine | Yes | Requires an explicit consent flag and a configured capture engine; returns external_dependency_required otherwise. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Persisted only after explicit consent/approval; sensitive proof payloads redacted until approved. |
| 26 | Proof log collection | low | allowed | Available | — | Yes | Gathers the real captured stdout/stderr/log lines produced during task execution. | Confirmed directly by the presence of the collected log content. | Persisted to the DB-backed memory/audit table, keyed by user_id + workspace_id; never mixed across tenants. |
| 27 | Proof command output collection | low | allowed | Available | — | Yes | Gathers the real captured command output produced during task execution. | Confirmed directly by the presence of the collected command output. | Persisted to the DB-backed memory/audit table, keyed by user_id + workspace_id; never mixed across tenants. |
| 28 | Confidence scoring | low | allowed | Available | — | No | Computes a rule-based score from how many independent checks (schema, output, state) agreed. | Self-verified: the score is derived transparently from the underlying check results returned alongside it. | Not persisted; produces an in-request check result without creating a durable record. |
| 29 | Partial success detection | medium | allowed | Available | — | No | Compares each expected outcome individually and reports a partial-success state when only some match. | Confirmed directly by the per-outcome comparison results. | Not persisted; produces an in-request check result without creating a durable record. |
| 30 | Visual regression check adapter | low | allowed | Needs external dependency | visual_regression_engine | No | Returns external_dependency_required until a visual regression engine is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; produces an in-request check result without creating a durable record. |
| 31 | Deployment verification | high | allowed | Available | — | Yes | Checks the real deployment command's exit code plus a post-deploy health-endpoint probe. | Confirmed directly by the deployment exit code and health-check response. | Persisted to the DB-backed memory/audit table, keyed by user_id + workspace_id; never mixed across tenants. |
| 32 | Mobile/emulator state check adapter | low | allowed | Needs external dependency | mobile_emulator_bridge | No | Returns external_dependency_required until a mobile/emulator bridge (per apps/worker_nodes/android) is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; produces an in-request check result without creating a durable record. |
| 33 | Download integrity check | medium | allowed | Available | — | No | Computes a real file hash/size and compares against the expected value when provided. | Confirmed directly by the computed checksum/size comparison. | Not persisted; produces an in-request check result without creating a durable record. |
| 34 | Completion report | low | allowed | Available | — | Yes | Compiles all individual check results for the task into one structured report. | Self-verified: the report is a direct compilation of already-confirmed check results. | Persisted to the DB-backed memory/audit table, keyed by user_id + workspace_id; never mixed across tenants. |
| 35 | Verification memory save | low | allowed | Available | — | Yes | Writes the verification result to the DB-backed memory table via the Memory Agent bridge. | Confirmed by re-reading back the saved row. | Persisted to the DB-backed memory/audit table, keyed by user_id + workspace_id; never mixed across tenants. |
| 36 | Verification pattern recall | low | allowed | Available | — | No | Queries scoped verification history from the Memory Agent. | Confirmed directly by the returned historical records. | Persisted to the DB-backed memory/audit table, keyed by user_id + workspace_id; never mixed across tenants. |
| 37 | Verification thresholds | low | allowed | Available | — | No | Reads a configurable threshold (default or per-workspace) and applies it to the computed confidence score. | Self-verified: threshold application is deterministic and shown alongside the result. | Persisted to the DB-backed memory/audit table, keyed by user_id + workspace_id; never mixed across tenants. |
| 38 | Retry limit enforcement | medium | allowed | Available | — | No | Tracks retry count per step and stops auto-retry once the configured limit is reached. | Confirmed directly by the tracked retry counter. | Held only for the lifetime of the active check/request; not persisted to durable storage. |
| 39 | Timeout enforcement | medium | allowed | Available | — | No | Applies a real wall-clock timeout to each check and marks it failed/unknown on expiry. | Confirmed directly by the elapsed-time measurement. | Held only for the lifetime of the active check/request; not persisted to durable storage. |
| 40 | Verification audit log | low | allowed | Available | — | Yes | Writes each verification outcome to the DB-backed audit log table. | Confirmed by re-reading back the written audit row. | Persisted to the DB-backed memory/audit table, keyed by user_id + workspace_id; never mixed across tenants. |
| 41 | Verification payload builder | low | allowed | Available | — | No | Assembles context + task_payload fields from core/verification_bridge.py into the expected structure. | Self-verified: the builder's own schema check confirms required fields are present. | Not persisted; produces an in-request check result without creating a durable record. |
| 42 | MasterAgent verification bridge | medium | allowed | Available | — | Yes | Invokes the Verification Agent via core/verification_bridge.py after execution and returns its structured result; note VerificationAgent.verify_task currently takes context and task_payload as separate arguments rather than one combined dict, a known real adapter gap some call sites may not yet handle correctly. | Confirmed directly by inspecting the returned verification result and bridge call success. | Not persisted; produces an in-request check result without creating a durable record. |
| 43 | Code Agent verification | medium | allowed | Available | — | Yes | Runs build/test/file-existence checks scoped to the Code Agent's reported changes. | Confirmed directly by build/test exit codes and file checks. | Persisted to the DB-backed memory/audit table, keyed by user_id + workspace_id; never mixed across tenants. |
| 44 | Browser Agent verification | low | allowed | Available | — | No | Cross-checks the Browser Agent's reported URL/title/content against the expected outcome. | Confirmed directly by Browser Agent state comparison. | Not persisted; produces an in-request check result without creating a durable record. |
| 45 | System Agent verification | medium | allowed | Available | — | Yes | Cross-checks System Agent reported process/file state against the expected outcome. | Confirmed directly by System Agent state comparison. | Not persisted; produces an in-request check result without creating a durable record. |
| 46 | Workflow Agent verification | medium | allowed | Available | — | Yes | Cross-checks Workflow Agent run logs/step results against the expected pipeline outcome. | Confirmed directly by Workflow Agent run-log comparison. | Persisted to the DB-backed memory/audit table, keyed by user_id + workspace_id; never mixed across tenants. |
| 47 | Finance safe verification | high | approval_required | Available | — | Yes | Cross-checks Finance Agent results against the approval record and rejects verification of any unapproved real transaction claim. | Confirmed directly by cross-referencing the Security Agent approval record. | Persisted only after explicit consent/approval; sensitive proof payloads redacted until approved. |
| 48 | Security decision verification | high | allowed | Available | — | Yes | Cross-checks the executed action against the recorded approval decision to confirm they match. | Confirmed directly by comparing the audit log's approval decision to the action outcome. | Not stored as memory; written only to the audit log table for compliance traceability. |
| 49 | Verification Agent health/dependency check | low | allowed | Available | — | No | Reports which check types are available today versus external_dependency_required. | Self-verified: reports its own adapter configuration state directly. | Not persisted; produces an in-request check result without creating a durable record. |
| 50 | Verification dashboard status | low | allowed | Available | — | No | Computes counts/aggregates from scoped verification history. | Confirmed directly by the underlying aggregate query. | Not persisted; produces an in-request check result without creating a durable record. |

## Visual Agent (`visual`)

**Purpose:** Screen/image/video understanding, OCR, UI mapping, privacy filtering, and visual validation.

**Example test prompt:** _Look at this screenshot and tell me what error is being shown._

| # | Name | Risk | Permission | Status | Required integrations | Audit | Safe MVP behavior | Verification method | Memory policy |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Screenshot understanding | low | allowed | Needs external dependency | image_analysis_engine | No | Returns external_dependency_required until an image/vision analysis engine is configured; does not fabricate a description. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 2 | Image understanding | low | allowed | Needs external dependency | image_analysis_engine | No | Returns external_dependency_required until an image/vision analysis engine is configured; does not fabricate a description. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 3 | OCR text extraction adapter | low | allowed | Needs external dependency | ocr_engine | No | Returns external_dependency_required until an OCR engine (e.g. Tesseract) is configured; never fabricates recognized text. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 4 | UI button detection | low | allowed | Needs external dependency | ui_element_detection_model | No | Returns external_dependency_required until a UI element detection model is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 5 | UI input detection | low | allowed | Needs external dependency | ui_element_detection_model | No | Returns external_dependency_required until a UI element detection model is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 6 | Form field detection | low | allowed | Needs external dependency | ui_element_detection_model, ocr_engine | No | Returns external_dependency_required until a UI element detection model plus OCR is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 7 | Dashboard layout mapping | low | allowed | Needs external dependency | ui_element_detection_model | No | Returns external_dependency_required until a UI element detection model is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 8 | Error popup detection | low | allowed | Needs external dependency | ocr_engine, ui_element_detection_model | No | Returns external_dependency_required until OCR and UI detection are configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 9 | Toast/modal detection | low | allowed | Needs external dependency | ui_element_detection_model | No | Returns external_dependency_required until a UI element detection model is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 10 | Video frame extraction | low | allowed | Needs external dependency | video_processing_engine | No | Returns external_dependency_required until a video processing engine is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 11 | Screen recording workflow timeline | low | allowed | Needs external dependency | video_processing_engine | No | Returns external_dependency_required until a video processing engine is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 12 | Click detection in videos | low | allowed | Needs external dependency | video_processing_engine, ui_element_detection_model | No | Returns external_dependency_required until a video processing engine and UI detection model are configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 13 | Scroll detection in videos | low | allowed | Needs external dependency | video_processing_engine | No | Returns external_dependency_required until a video processing engine is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 14 | Visual workflow learner | medium | allowed | Needs external dependency | video_processing_engine, ui_element_detection_model | Yes | Returns external_dependency_required until video processing and UI detection are configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 15 | Element bounds for automation | low | allowed | Needs external dependency | ui_element_detection_model | No | Returns external_dependency_required until a UI element detection model is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 16 | Before/after screenshot compare | medium | allowed | Available | — | No | Computes a real pixel/structural diff between the two supplied images using local image-diff logic. | VerificationAgent confirms the diff regions reported are non-empty/consistent with the reported change. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 17 | Landing page visual audit | low | allowed | Needs external dependency | image_analysis_engine | No | Returns external_dependency_required until an image analysis engine is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 18 | Design spacing feedback | low | allowed | Needs external dependency | ui_element_detection_model | No | Returns external_dependency_required until a UI element detection model is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 19 | Alignment feedback | low | allowed | Needs external dependency | ui_element_detection_model | No | Returns external_dependency_required until a UI element detection model is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 20 | Color contrast checks | medium | allowed | Available | — | No | Computes real WCAG contrast ratios from supplied foreground/background color values or sampled pixels. | VerificationAgent recomputes the contrast ratio from the same inputs and compares. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 21 | Accessibility visual hints | low | allowed | Available | — | No | Applies rule-based accessibility heuristics to whatever structural/metadata info is available. | VerificationAgent confirms hints match the rule-based checklist applied. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 22 | Sensitive data detection | high | allowed | Needs external dependency | ocr_engine | Yes | Returns external_dependency_required until an OCR engine is configured; never assumes text content. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 23 | Visual privacy redaction | high | approval_required | Needs external dependency | ocr_engine, image_analysis_engine | Yes | Returns external_dependency_required until OCR/detection is configured; never stores an unredacted sensitive image. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Persisted only after explicit consent/approval; raw sensitive imagery is redacted or discarded until approved. |
| 24 | App screen maps | low | allowed | Needs external dependency | ui_element_detection_model | No | Returns external_dependency_required until a UI element detection model is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 25 | Chrome screen map | low | allowed | Needs external dependency | ui_element_detection_model | No | Returns external_dependency_required until a UI element detection model is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 26 | VS Code screen map | low | allowed | Needs external dependency | ui_element_detection_model | No | Returns external_dependency_required until a UI element detection model is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 27 | WordPress screen map | low | allowed | Needs external dependency | ui_element_detection_model | No | Returns external_dependency_required until a UI element detection model is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 28 | Google Ads screen map | low | allowed | Needs external dependency | ui_element_detection_model | No | Returns external_dependency_required until a UI element detection model is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 29 | Google Maps card detection | low | allowed | Needs external dependency | ui_element_detection_model, ocr_engine | No | Returns external_dependency_required until a UI element detection model and OCR are configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 30 | Mobile screen understanding | low | allowed | Needs external dependency | image_analysis_engine, mobile_emulator_bridge | No | Returns external_dependency_required until an image analysis engine and mobile bridge are configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 31 | Emulator screen understanding | low | allowed | Needs external dependency | image_analysis_engine, mobile_emulator_bridge | No | Returns external_dependency_required until an image analysis engine and mobile/emulator bridge are configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 32 | Annotation/report screenshots | medium | allowed | Available | — | No | Draws provided annotation coordinates onto the supplied image using local image-drawing logic. | VerificationAgent confirms the output image dimensions/annotation count match the request. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 33 | Draw boxes around elements | medium | allowed | Available | — | No | Draws the supplied bounding-box coordinates onto the image using local image-drawing logic. | VerificationAgent confirms box count/coordinates in the output match the input. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 34 | Visual validation handoff | medium | allowed | Available | — | No | Packages the visual result into the Verification Agent's expected payload and forwards it. | Confirmed by the Verification Agent's own acceptance of the payload schema. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 35 | Visual error routing to Code Agent | medium | allowed | Available | — | Yes | Forwards the detected error context/screenshot reference to the Code Agent's task queue. | Confirmed by the Code Agent's task-received acknowledgment. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 36 | Visual privacy policy enforcement | high | blocked_by_default | Available | — | Yes | Checks capture requests against a rule-based denylist (banking apps, password managers) and refuses matching captures. | VerificationAgent confirms no image artifact exists for a blocked request. | Not stored as memory; written only to the audit log table for compliance traceability. |
| 37 | Screenshot consent enforcement | high | blocked_by_default | Available | — | Yes | Rejects capture requests lacking an explicit consent flag on the task. | VerificationAgent confirms the consent flag was present before any capture record exists. | Not stored as memory; written only to the audit log table for compliance traceability. |
| 38 | Visual memory save | low | allowed | Available | — | Yes | Writes the structured analysis result to the DB-backed memory table via the Memory Agent bridge. | Confirmed by re-reading back the saved row. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 39 | Repeated layout recognition | low | allowed | Available | — | No | Compares structural fingerprints of the current screenshot against stored layout fingerprints in memory. | VerificationAgent confirms the matched fingerprint ID exists in memory. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 40 | UI confidence scoring | low | allowed | Available | — | No | Computes a rule-based score from detection-adapter confidence plus contextual heuristics. | Self-verified: score is derived transparently and returned alongside the detection result. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 41 | Table/card detection | low | allowed | Needs external dependency | ui_element_detection_model | No | Returns external_dependency_required until a UI element detection model is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 42 | Logo/brand asset detection | low | allowed | Needs external dependency | image_analysis_engine | No | Returns external_dependency_required until an image analysis engine is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 43 | Creative image analysis | low | allowed | Needs external dependency | image_analysis_engine | No | Returns external_dependency_required until an image analysis engine is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 44 | Ad creative review | medium | allowed | Needs external dependency | image_analysis_engine | Yes | Returns external_dependency_required until an image analysis engine is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 45 | Screenshot proof packaging | medium | approval_required | Available | — | Yes | Bundles an already-captured, consented screenshot with timestamp/context metadata. | VerificationAgent confirms the proof package's metadata matches the originating task. | Persisted only after explicit consent/approval; raw sensitive imagery is redacted or discarded until approved. |
| 46 | Image metadata extraction | low | allowed | Available | — | No | Reads real image metadata using local image-library parsing (dimensions, format, EXIF if present). | VerificationAgent confirms extracted metadata matches the file's actual header data. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 47 | Redacted proof export | medium | approval_required | Available | — | Yes | Applies known redaction regions and exports only after explicit approval; refuses export of unredacted sensitive images. | VerificationAgent confirms the exported file excludes the flagged sensitive regions. | Persisted only after explicit consent/approval; raw sensitive imagery is redacted or discarded until approved. |
| 48 | Visual Agent health/dependency check | low | allowed | Available | — | No | Reports which visual capabilities are available today versus external_dependency_required. | Self-verified: reports its own adapter configuration state directly. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |
| 49 | Visual task audit log | low | allowed | Available | — | Yes | Writes a scoped audit record (task type, consent flag, outcome) to the DB-backed audit log table. | Confirmed by re-reading back the written audit row. | Not stored as memory; written only to the audit log table for compliance traceability. |
| 50 | Visual-to-System automation bridge | medium | allowed | Available | — | Yes | Forwards validated element bounds to the System Agent's automation queue; does not perform system-level input itself. | Confirmed by the System Agent's task-received acknowledgment and its own action verification. | Not persisted; operates on the supplied image/screenshot without creating a durable record. |

## Workflow Agent (`workflow`)

**Purpose:** Automation pipelines, webhooks, triggers, n8n, forms, sheets, WhatsApp, CRM, scheduling, monitoring.

**Example test prompt:** _Build a workflow that adds new form submissions to our CRM and sends a WhatsApp alert._

| # | Name | Risk | Permission | Status | Required integrations | Audit | Safe MVP behavior | Verification method | Memory policy |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Build trigger-action pipelines | low | allowed | Available | — | No | Generates a structured, validated pipeline plan (trigger, conditions, actions) as local data; does not execute it. | VerificationAgent confirms the generated plan matches the workflow-plan schema. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 2 | Workflow plan JSON generation | low | allowed | Available | — | No | Serializes the built pipeline plan to validated JSON. | VerificationAgent confirms the generated plan matches the workflow-plan schema. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 3 | n8n workflow generation | low | allowed | Needs external dependency | n8n_instance_url | No | Produces the n8n JSON structure locally; does not deploy it until an n8n instance is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 4 | n8n connector adapter | low | allowed | Needs external dependency | n8n_instance_url, n8n_api_key | No | Returns external_dependency_required until an n8n instance URL/API key is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; produces an in-request plan/result without creating a durable record. |
| 5 | Webhook endpoint planning | low | allowed | Available | — | No | Generates a validated webhook contract as local data; does not expose a live endpoint by itself. | VerificationAgent confirms the generated plan matches the workflow-plan schema. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 6 | Webhook validation | medium | allowed | Available | — | No | Runs a real structural/signature validation against the supplied payload. | Confirmed directly by the validation pass/fail result. | Not persisted; produces an in-request plan/result without creating a durable record. |
| 7 | Form-to-Sheet pipeline | low | allowed | Needs external dependency | google_sheets_api_credentials | No | Generates and simulates the pipeline plan; live writes require Google Sheets credentials to be configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 8 | Form-to-WhatsApp pipeline | low | allowed | Needs external dependency | whatsapp_business_api_credentials | No | Generates and simulates the pipeline plan; live sends require WhatsApp Business API credentials. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 9 | Form-to-CRM pipeline | low | allowed | Needs external dependency | crm_api_credentials | No | Generates and simulates the pipeline plan; live writes require CRM API credentials. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 10 | Google Sheets connector adapter | low | allowed | Needs external dependency | google_sheets_api_credentials | No | Returns external_dependency_required until Google Sheets API credentials are configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; produces an in-request plan/result without creating a durable record. |
| 11 | Airtable connector adapter | low | allowed | Needs external dependency | airtable_api_key | No | Returns external_dependency_required until an Airtable API key is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; produces an in-request plan/result without creating a durable record. |
| 12 | Excel connector adapter | low | allowed | Needs external dependency | excel_connector | No | Returns external_dependency_required until an Excel connector is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; produces an in-request plan/result without creating a durable record. |
| 13 | CRM connector | low | allowed | Needs external dependency | crm_api_credentials | No | Returns external_dependency_required until CRM API credentials are configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; produces an in-request plan/result without creating a durable record. |
| 14 | WhatsApp notification connector | low | allowed | Needs external dependency | whatsapp_business_api_credentials | No | Returns external_dependency_required until WhatsApp Business API credentials are configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; produces an in-request plan/result without creating a durable record. |
| 15 | Email connector | low | allowed | Needs external dependency | smtp_credentials | No | Returns external_dependency_required until SMTP credentials are configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; produces an in-request plan/result without creating a durable record. |
| 16 | Slack notification connector | low | allowed | Needs external dependency | slack_webhook_url | No | Returns external_dependency_required until a Slack webhook URL is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; produces an in-request plan/result without creating a durable record. |
| 17 | Discord notification connector | low | allowed | Needs external dependency | discord_webhook_url | No | Returns external_dependency_required until a Discord webhook URL is configured. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Not persisted; produces an in-request plan/result without creating a durable record. |
| 18 | Conditional if/else logic | low | allowed | Available | — | No | Evaluates real boolean expressions against provided step-output data using local logic. | Confirmed directly by the evaluated branch result. | Not persisted; produces an in-request plan/result without creating a durable record. |
| 19 | Lead scoring | medium | allowed | Available | — | No | Applies a rule-based scoring formula (source, fields completed, engagement) to the lead payload. | VerificationAgent confirms the score matches recomputation from the same inputs. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 20 | Duplicate checking | medium | allowed | Available | — | No | Runs a scoped DB lookup/similarity match against existing records. | Confirmed directly by the DB lookup result. | Not persisted; produces an in-request plan/result without creating a durable record. |
| 21 | Spam filtering | medium | allowed | Available | — | No | Applies rule-based spam heuristics (honeypot fields, known bad patterns, rate limits). | Confirmed directly by the heuristic match result. | Not persisted; produces an in-request plan/result without creating a durable record. |
| 22 | Scheduled workflows | medium | allowed | Available | — | Yes | Registers a scoped schedule entry that the task manager checks and triggers at the configured time. | VerificationAgent confirms a run-log entry was created at the scheduled time. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 23 | Delayed actions | low | allowed | Available | — | No | Schedules the action for future execution via the task manager rather than running it immediately. | VerificationAgent confirms the action executed at/after the configured delay. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 24 | Workflow monitor | low | allowed | Available | — | No | Polls scoped run-status records and flags runs exceeding expected duration or erroring. | Confirmed directly by the polled run-status data. | Not persisted; produces an in-request plan/result without creating a durable record. |
| 25 | Failed-step diagnosis | medium | allowed | Available | — | No | Inspects the step's captured error/output and matches it against known-cause heuristics. | Confirmed directly by the inspected step error data. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 26 | Safe retries | medium | approval_required | Available | — | Yes | Retries only steps tagged non-sensitive, up to a configured retry limit, logging every attempt. | Confirmed by re-checking the step's own postcondition after retry. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 27 | No duplicate lead retry | medium | blocked_by_default | Available | — | Yes | Rechecks for an existing matching record before any retry-created write and skips the write if one already exists. | Confirmed directly by the duplicate-check result before the retry write. | Not persisted; produces an in-request plan/result without creating a durable record. |
| 28 | Approval gates | high | approval_required | Available | — | Yes | Halts execution at the gate step and creates a Security Agent approval request; resumes only on approval. | Confirmed directly by the approval-request record's resolved state. | Persisted only as part of an approval-request record; live-send/live-write details withheld until resolved. |
| 29 | Workflow templates | low | allowed | Available | — | No | Returns a library of locally stored, validated template plans. | VerificationAgent confirms the returned template matches the stored template schema. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 30 | Plain-language workflow builder | low | allowed | Available | — | No | Parses the description into a structured plan using rule-based/keyword mapping; does not execute it. | VerificationAgent confirms the generated plan matches the workflow-plan schema. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 31 | n8n JSON exporter | low | allowed | Available | — | No | Serializes the local pipeline plan into n8n's JSON node/connection format. | VerificationAgent confirms the exported JSON parses as valid n8n workflow format. | Not persisted; produces an in-request plan/result without creating a durable record. |
| 32 | API connector generation | medium | allowed | Available | — | No | Produces a structured connector definition (endpoint, auth type, mapping) from provided API details; does not call the API until credentials are supplied. | VerificationAgent confirms the connector definition matches the expected schema. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 33 | Workflow health dashboard | low | allowed | Available | — | No | Computes counts/aggregates from scoped run-log records. | Confirmed directly by the underlying aggregate query. | Not persisted; produces an in-request plan/result without creating a durable record. |
| 34 | Hot lead alerts | medium | allowed | Needs external dependency | slack_webhook_url, whatsapp_business_api_credentials | Yes | Detects the threshold crossing locally; live notification delivery requires a configured notification connector. | N/A while capability_status is external_dependency_required; verification is skipped and surfaced as such. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 35 | Client onboarding automation | medium | allowed | Available | — | Yes | Generates and can dry-run the onboarding pipeline plan; live external steps require their respective connectors. | VerificationAgent confirms each onboarding step's postcondition. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 36 | Support ticket routing | medium | allowed | Available | — | No | Applies rule-based routing criteria (keywords, priority, category) to the ticket payload. | Confirmed directly by the routing-rule match result. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 37 | Payment reminder workflows with approval | high | approval_required | Approval required | — | Yes | Generates the reminder plan and blocks the live send step until Security Agent approval is recorded. | Confirmed directly by the approval-request record's resolved state. | Persisted only as part of an approval-request record; live-send/live-write details withheld until resolved. |
| 38 | Workflow memory | low | allowed | Available | — | No | Writes workflow plans/run summaries to the DB-backed memory table via the Memory Agent bridge. | Confirmed by re-reading back the saved row. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 39 | Connected tools registry | low | allowed | Available | — | No | Reads configured-integration flags and reports them without exposing raw credentials. | Confirmed directly by the configuration-flag read. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 40 | Workflow run logs | low | allowed | Available | — | Yes | Writes a scoped run-log record to the DB for every execution/dry-run. | Confirmed by re-reading back the written run-log row. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 41 | Workflow event WebSocket | low | allowed | Available | — | No | Publishes scoped workflow run events onto the existing WebSocket channel as they occur. | Confirmed directly by the published event matching the run-log record. | Held only for the lifetime of the active dry-run/simulation; not persisted to durable storage. |
| 42 | Workflow dry-run mode | low | allowed | Available | — | No | Runs each step's logic against simulated/sample data and reports what would have happened. | VerificationAgent confirms no live connector call was made during the dry run. | Held only for the lifetime of the active dry-run/simulation; not persisted to durable storage. |
| 43 | Workflow simulation mode | low | allowed | Available | — | No | Runs the plan against supplied test data through the same step logic, without touching live external systems. | VerificationAgent confirms all steps report simulated, not live, execution. | Held only for the lifetime of the active dry-run/simulation; not persisted to durable storage. |
| 44 | Workflow rollback plan | medium | allowed | Available | — | No | Derives a rollback plan from each step's declared inverse action; does not execute the rollback automatically. | VerificationAgent confirms the rollback plan covers every step with a recorded state change. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |
| 45 | Workflow permission policy | medium | allowed | Available | — | Yes | Checks the caller's role/permission against the Security Agent's per-agent policy before allowing the action. | Confirmed directly by the Security Agent policy check result. | Not stored as memory; written only to the audit log table for compliance traceability. |
| 46 | Workflow secret reference checker | high | blocked_by_default | Available | — | Yes | Scans generated workflow JSON for literal secret-shaped values and rejects the plan if found, requiring a secrets_manager reference instead. | Confirmed directly by the scan result on the generated plan. | Not stored as memory; written only to the audit log table for compliance traceability. |
| 47 | Workflow report builder | low | allowed | Available | — | No | Compiles scoped run-log and metric data into a structured report. | Confirmed directly by the underlying report query. | Not persisted; produces an in-request plan/result without creating a durable record. |
| 48 | Workflow Agent health/dependency check | low | allowed | Available | — | No | Reports DB connectivity and which named integrations are configured versus missing. | Self-verified: reports its own configuration state directly. | Not persisted; produces an in-request plan/result without creating a durable record. |
| 49 | Workflow audit log | low | allowed | Available | — | Yes | Writes a scoped audit record for every workflow lifecycle event to the DB-backed audit log table. | Confirmed by re-reading back the written audit row. | Not stored as memory; written only to the audit log table for compliance traceability. |
| 50 | Workflow-to-Business handoff | medium | allowed | Available | — | Yes | Forwards the structured workflow result to the Business Agent's task queue. | Confirmed by the Business Agent's task-received acknowledgment. | Persisted to the DB-backed memory table via the Memory Agent bridge, keyed by user_id + workspace_id. |

## Hologram Agent (`hologram`)

**Purpose:** AR/glasses/spatial overlay brain with safe structured output when hardware is unavailable.

**Example test prompt:** _Generate an AR overlay card showing today's top 3 tasks._

| # | Name | Risk | Permission | Status | Required integrations | Audit | Safe MVP behavior | Verification method | Memory policy |
|---|---|---|---|---|---|---|---|---|---|
| 1 | AR overlay card generation | low | allowed | Available | — | No | Generates and returns overlay card JSON locally; no AR device is required to produce it. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Held only for the lifetime of the active AR session/display; not persisted to durable storage. |
| 2 | Floating task cards | low | allowed | Available | — | No | Builds a floating task-card JSON payload from the user's active task list. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Held only for the lifetime of the active AR session/display; not persisted to durable storage. |
| 3 | Real-world object detection adapter | medium | allowed | Needs external dependency | ar_camera_hardware, object_detection_model | No | Returns an honest capability_unavailable response until AR camera hardware and a detection model are configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on already-available data rather than creating new stored records. |
| 4 | Spatial mapping adapter | medium | allowed | Needs external dependency | ar_hardware_sdk, spatial_mapping_sdk | No | Returns an honest capability_unavailable response until an AR hardware SDK and spatial mapping SDK are configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on already-available data rather than creating new stored records. |
| 5 | Gesture control adapter | low | allowed | Needs external dependency | ar_hardware_sdk, gesture_recognition_sdk | No | Returns an honest capability_unavailable response until an AR hardware SDK and gesture recognition SDK are configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on already-available data rather than creating new stored records. |
| 6 | Glass tap wake command | low | allowed | Needs external dependency | ar_hardware_sdk | No | Returns an honest capability_unavailable response until an AR hardware SDK is configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Held only for the lifetime of the active AR session/display; not persisted to durable storage. |
| 7 | Real-world navigation arrows | low | allowed | Available | — | No | Computes a navigation-arrow overlay plan (bearing/distance) as JSON from provided coordinates. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Held only for the lifetime of the active AR session/display; not persisted to durable storage. |
| 8 | Meeting overlays | low | allowed | Available | — | No | Builds a meeting overlay card JSON from provided meeting context. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Held only for the lifetime of the active AR session/display; not persisted to durable storage. |
| 9 | Notification overlays | low | allowed | Available | — | No | Builds a notification overlay card JSON from the pending notification payload. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Held only for the lifetime of the active AR session/display; not persisted to durable storage. |
| 10 | Translation captions in AR | medium | allowed | Available | — | No | Formats already-translated caption text into an overlay card JSON; does not itself perform translation. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Held only for the lifetime of the active AR session/display; not persisted to durable storage. |
| 11 | Device status overlay | low | allowed | Available | — | No | Builds a device-status card JSON from device status data already reported to the system. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Not persisted; operates on already-available data rather than creating new stored records. |
| 12 | Repair/setup guidance | low | allowed | Available | — | No | Generates a structured step-by-step guidance card sequence from a known procedure definition. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Held only for the lifetime of the active AR session/display; not persisted to durable storage. |
| 13 | Live instruction cards | low | allowed | Available | — | No | Builds live instruction-card JSON reflecting the current task step. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Held only for the lifetime of the active AR session/display; not persisted to durable storage. |
| 14 | Privacy redaction for bystanders | medium | allowed | Available | — | Yes | Applies a rule-based redaction pass to any bystander-identifying fields before an overlay is generated or logged. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Not persisted; operates on already-available data rather than creating new stored records. |
| 15 | No hidden monitoring | low | allowed | Available | — | Yes | Enforces and reports an always-visible recording-state indicator policy; refuses silent capture modes. | VerificationAgent confirms a matching audit log row was written for this action. | Not persisted; operates on already-available data rather than creating new stored records. |
| 16 | Workstation overlay | low | allowed | Available | — | No | Builds a workstation overlay card JSON from the user's current active-app context. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Held only for the lifetime of the active AR session/display; not persisted to durable storage. |
| 17 | CRM/client cards in AR | medium | allowed | Available | — | Yes | Builds a client info card JSON from real CRM records if present, otherwise an honest empty-state card. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 18 | Call info overlay | medium | allowed | Available | — | Yes | Builds a call-info overlay card JSON from data supplied by the Call Agent for the active call. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Held only for the lifetime of the active AR session/display; not persisted to durable storage. |
| 19 | Content shot framing guide | low | allowed | Available | — | No | Builds a framing-guide overlay card JSON (grid/subject markers) for the current shot. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Held only for the lifetime of the active AR session/display; not persisted to durable storage. |
| 20 | Memory recall cards in AR | medium | allowed | Available | — | Yes | Builds a memory-recall card JSON from records returned by the Memory Agent for the scoped user/workspace. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 21 | Smart glasses device bridge | medium | allowed | Needs external dependency | ar_hardware_sdk, smart_glasses_vendor_api | No | Returns an honest capability_unavailable response until a smart glasses vendor SDK is configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on already-available data rather than creating new stored records. |
| 22 | Watch/phone/laptop stream bridge | medium | allowed | Needs external dependency | device_streaming_sdk | No | Returns an honest capability_unavailable response until a device streaming SDK is configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on already-available data rather than creating new stored records. |
| 23 | AR safe zone settings | low | allowed | Available | — | Yes | Reads/writes a per-user safe-zone settings record via the DB-backed memory table. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 24 | Public/private AR mode | low | allowed | Available | — | Yes | Reads/writes a per-user public/private AR mode setting. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 25 | AR notification filtering | low | allowed | Available | — | No | Applies a per-user notification-filter rule set before generating overlay cards. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 26 | AR task progress timeline | low | allowed | Available | — | No | Builds a progress-timeline card JSON from tracked task-step state. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Held only for the lifetime of the active AR session/display; not persisted to durable storage. |
| 27 | AR proof display | low | allowed | Available | — | No | Builds a proof-of-completion card JSON from the verification result already produced by VerificationAgent. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Not persisted; operates on already-available data rather than creating new stored records. |
| 28 | AR command confirmation | medium | allowed | Available | — | Yes | Builds a confirmation-card JSON requiring explicit acknowledgment before the underlying command proceeds. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Held only for the lifetime of the active AR session/display; not persisted to durable storage. |
| 29 | AR gesture approval | medium | approval_required | Approval required | — | Yes | Blocks the gesture-triggered action and displays an approval card until the user/Security Agent confirms. | VerificationAgent confirms a matching audit log row was written for this action. | Held only for the lifetime of the active AR session/display; not persisted to durable storage. |
| 30 | AR object label confidence | low | allowed | Needs external dependency | ar_camera_hardware, object_detection_model | No | Formats a confidence score onto detection output when the object detection adapter is available; otherwise returns capability_unavailable. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on already-available data rather than creating new stored records. |
| 31 | AR navigation instruction plan | low | allowed | Available | — | No | Computes a step-by-step navigation plan as JSON from provided route data. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Held only for the lifetime of the active AR session/display; not persisted to durable storage. |
| 32 | AR overlay export JSON | low | allowed | Available | — | No | Serializes the current overlay state to a JSON document for export. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Not persisted; operates on already-available data rather than creating new stored records. |
| 33 | AR layout memory | low | allowed | Available | — | No | Stores/reads the layout preference via the DB-backed memory table. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 34 | AR recurring overlay preferences | low | allowed | Available | — | No | Stores/reads recurring overlay preference records via the DB-backed memory table. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 35 | AR device dependency check | low | allowed | Available | — | No | Inspects configuration/environment for AR hardware SDK presence and reports an honest configured/not-configured status. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Not persisted; operates on already-available data rather than creating new stored records. |
| 36 | AR privacy audit | medium | allowed | Available | — | Yes | Reads from the audit log table filtered to AR/hologram-category events and reports compliance. | VerificationAgent confirms a matching audit log row was written for this action. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 37 | AR bystander protection | high | allowed | Available | — | Yes | Applies the bystander-redaction rule set to all overlay/detection output and blocks identification without consent. | VerificationAgent confirms a matching audit log row was written for this action. | Not persisted; operates on already-available data rather than creating new stored records. |
| 38 | AR language caption settings | low | allowed | Available | — | No | Reads/writes a per-user caption language preference record. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 39 | AR meeting summary cards | low | allowed | Available | — | No | Builds a meeting summary card JSON from provided meeting notes/transcript text. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Held only for the lifetime of the active AR session/display; not persisted to durable storage. |
| 40 | AR workflow guidance | low | allowed | Available | — | No | Builds workflow-step overlay cards JSON from the Workflow Agent's step definitions. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Held only for the lifetime of the active AR session/display; not persisted to durable storage. |
| 41 | AR browser research cards | low | allowed | Available | — | No | Builds a research-summary card JSON from results supplied by the Browser Agent. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Held only for the lifetime of the active AR session/display; not persisted to durable storage. |
| 42 | AR system status card | low | allowed | Available | — | No | Builds a system-status card JSON from data supplied by the System Agent. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Not persisted; operates on already-available data rather than creating new stored records. |
| 43 | AR finance warning card | medium | allowed | Available | — | Yes | Builds a finance-warning card JSON from an alert supplied by the Finance Agent; never displays fabricated figures. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Not persisted; operates on already-available data rather than creating new stored records. |
| 44 | AR security approval card | high | approval_required | Available | — | Yes | Builds a security-approval card JSON reflecting a pending Security Agent approval request; the underlying action does not proceed until approved. | VerificationAgent confirms a matching audit log row was written for this action. | Not persisted; operates on already-available data rather than creating new stored records. |
| 45 | AR handoff to Visual Agent | low | allowed | Available | — | No | Forwards a structured task payload to the Visual Agent and returns its response. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Not persisted; operates on already-available data rather than creating new stored records. |
| 46 | AR handoff to Voice Agent | low | allowed | Available | — | No | Forwards a structured task payload to the Voice Agent and returns its response. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Not persisted; operates on already-available data rather than creating new stored records. |
| 47 | AR unavailable hardware response | low | allowed | Available | — | No | Detects missing AR hardware and returns a capability_unavailable response instead of simulating success. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Not persisted; operates on already-available data rather than creating new stored records. |
| 48 | Hologram Agent health/dependency check | low | allowed | Available | — | No | Reports process health and whether optional AR hardware SDKs are configured. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Not persisted; operates on already-available data rather than creating new stored records. |
| 49 | Hologram audit log | low | allowed | Available | — | No | Reads from the audit log table filtered to hologram-category events. | VerificationAgent confirms a matching audit log row was written for this action. | Stored in the DB-backed memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 50 | Hologram dashboard preview | low | allowed | Available | — | No | Computes a lightweight preview summary of active overlay cards for the dashboard. | VerificationAgent confirms the response matches the normalized overlay/card result schema. | Not persisted; operates on already-available data rather than creating new stored records. |

## Call Agent (`call`)

**Purpose:** AI receptionist, call consent, lead qualification, summaries, CRM handoff, appointment booking.

**Example test prompt:** _Draft a receptionist script for after-hours calls and qualify this lead transcript._

| # | Name | Risk | Permission | Status | Required integrations | Audit | Safe MVP behavior | Verification method | Memory policy |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Incoming call detection adapter | medium | allowed | Needs external dependency | telephony_provider_api_key | No | Returns an honest capability_unavailable response until a telephony provider (e.g. Twilio) is configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on already-available call/transcript data rather than creating new stored records. |
| 2 | Outgoing call detection adapter | medium | allowed | Needs external dependency | telephony_provider_api_key | No | Returns an honest capability_unavailable response until a telephony provider is configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on already-available call/transcript data rather than creating new stored records. |
| 3 | Business auto-answer only when enabled | high | approval_required | Approval required | telephony_provider_api_key | Yes | Auto-answer stays disabled until the user explicitly enables it and a telephony provider is configured; never answers silently. | VerificationAgent confirms a matching audit log row was written for this action. | Persisted/sent only after explicit user/Security Agent approval or caller consent; refused otherwise. |
| 4 | Caller ID lookup adapter | medium | allowed | Needs external dependency | telephony_provider_api_key, caller_id_lookup_api | No | Returns an honest capability_unavailable response until a telephony/caller-ID lookup provider is configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on already-available call/transcript data rather than creating new stored records. |
| 5 | Business hours routing | low | allowed | Available | — | No | Evaluates the current time against a stored business-hours schedule and returns a routing decision. | VerificationAgent confirms the response matches the normalized call/result schema. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 6 | Known contact recognition | medium | allowed | Available | — | Yes | Matches caller info against real CRM contact records if present, otherwise returns an honest unknown-caller result. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 7 | VIP caller recognition | medium | allowed | Available | — | Yes | Matches caller info against a stored VIP tag list. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 8 | Receptionist greeting by business profile | low | allowed | Available | — | No | Generates greeting text from the stored business profile record. | VerificationAgent confirms the response matches the normalized call/result schema. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 9 | Caller intent detection | low | allowed | Available | — | No | Runs a local rule/NLP classifier over the provided transcript text to label intent. | VerificationAgent confirms the response matches the normalized call/result schema. | Not persisted; operates on already-available call/transcript data rather than creating new stored records. |
| 10 | Service matching | low | allowed | Available | — | No | Matches transcript keywords against the stored service catalog. | VerificationAgent confirms the response matches the normalized call/result schema. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 11 | Transfer/escalation rules | medium | allowed | Available | — | Yes | Evaluates stored escalation rules against call context and returns a transfer decision. | VerificationAgent confirms the response matches the normalized call/result schema. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 12 | Lead qualification | medium | allowed | Available | — | Yes | Runs a rule-based qualification scorer over the provided transcript/answers. | VerificationAgent confirms the response matches the normalized call/result schema. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 13 | Budget question flow | medium | allowed | Available | — | Yes | Generates the budget question and records the caller's stated answer to the lead record. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 14 | Service question flow | low | allowed | Available | — | No | Generates the service question and records the caller's stated answer to the lead record. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 15 | Location question flow | medium | allowed | Available | — | Yes | Generates the location question and records the caller's stated answer to the lead record. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 16 | Urgency question flow | low | allowed | Available | — | No | Generates the urgency question and records the caller's stated answer to the lead record. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 17 | Appointment intent detection | low | allowed | Available | — | No | Classifies the provided transcript text for appointment-booking intent. | VerificationAgent confirms the response matches the normalized call/result schema. | Not persisted; operates on already-available call/transcript data rather than creating new stored records. |
| 18 | Calendar availability check | medium | allowed | Needs external dependency | calendar_provider_api_key | No | Returns an honest capability_unavailable response until a calendar provider is configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on already-available call/transcript data rather than creating new stored records. |
| 19 | Appointment booking with confirmation | high | approval_required | Approval required | calendar_provider_api_key | Yes | Booking proceeds only after explicit user/business confirmation and a configured calendar provider; never fabricates a booked slot. | VerificationAgent confirms a matching audit log row was written for this action. | Persisted/sent only after explicit user/Security Agent approval or caller consent; refused otherwise. |
| 20 | Voicemail capture with consent | high | approval_required | Approval required | telephony_provider_api_key | Yes | Requires an explicit consent flag on the request before capturing voicemail; refuses to record without it. | VerificationAgent confirms a matching audit log row was written for this action. | Persisted/sent only after explicit user/Security Agent approval or caller consent; refused otherwise. |
| 21 | Missed-call recovery | low | allowed | Available | — | No | Generates a follow-up message draft for a missed-call record; sending requires separate approval. | VerificationAgent confirms the response matches the normalized call/result schema. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 22 | Call transcription with legal/user consent | high | approval_required | Approval required | speech_to_text_provider | Yes | Requires an explicit consent flag and a configured transcription provider before transcribing; refuses otherwise. | VerificationAgent confirms a matching audit log row was written for this action. | Persisted/sent only after explicit user/Security Agent approval or caller consent; refused otherwise. |
| 23 | Live call summary | low | allowed | Available | — | No | Summarizes the provided live transcript text using local summarization. | VerificationAgent confirms the response matches the normalized call/result schema. | Held only for the lifetime of the active call/session; not persisted to durable storage. |
| 24 | Post-call action items | low | allowed | Available | — | No | Extracts action items from the provided transcript text via local rule/NLP parsing. | VerificationAgent confirms the response matches the normalized call/result schema. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 25 | CRM lead creation | medium | allowed | Available | — | Yes | Creates a real lead record in the DB-backed CRM table if the model exists, otherwise returns an honest not-configured result — never fabricates a lead. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 26 | CRM call log | medium | allowed | Available | — | Yes | Writes a real call-log row to the DB-backed CRM activity table if present, otherwise returns an honest empty state. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 27 | Call tagging | low | allowed | Available | — | No | Writes tag values onto the scoped call-log record. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 28 | Lead/support/client/vendor classification | low | allowed | Available | — | No | Runs a rule-based classifier over caller/transcript data to assign a relationship-type tag. | VerificationAgent confirms the response matches the normalized call/result schema. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 29 | Human escalation | medium | allowed | Available | — | Yes | Flags the call record for human handoff and returns the assigned escalation target; does not itself place a call. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 30 | Spam/robocall detection | low | allowed | Available | — | No | Applies a rule-based heuristic to whatever caller-pattern data is available to flag likely spam. | VerificationAgent confirms the response matches the normalized call/result schema. | Not persisted; operates on already-available call/transcript data rather than creating new stored records. |
| 31 | Language auto-detection | low | allowed | Available | — | No | Runs local language detection over the provided transcript/text. | VerificationAgent confirms the response matches the normalized call/result schema. | Not persisted; operates on already-available call/transcript data rather than creating new stored records. |
| 32 | Bilingual receptionist mode | low | allowed | Available | — | No | Selects the matching pre-authored script language based on detected language. | VerificationAgent confirms the response matches the normalized call/result schema. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 33 | Call script selection | low | allowed | Available | — | No | Selects a script from the stored script library based on call context/intent. | VerificationAgent confirms the response matches the normalized call/result schema. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 34 | Objection handling script | low | allowed | Available | — | No | Selects/generates an objection-handling script from the stored script library. | VerificationAgent confirms the response matches the normalized call/result schema. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 35 | Follow-up WhatsApp with approval | high | approval_required | Approval required | whatsapp_business_api | Yes | Drafts the WhatsApp message and sends only after explicit user/Security Agent approval and a configured WhatsApp provider. | VerificationAgent confirms a matching audit log row was written for this action. | Persisted/sent only after explicit user/Security Agent approval or caller consent; refused otherwise. |
| 36 | Follow-up email with approval | high | approval_required | Approval required | email_provider_api_key | Yes | Drafts the follow-up email and sends only after explicit approval and a configured email provider. | VerificationAgent confirms a matching audit log row was written for this action. | Persisted/sent only after explicit user/Security Agent approval or caller consent; refused otherwise. |
| 37 | Call recording consent announcement | high | approval_required | Approval required | — | Yes | Plays/states the recording-consent announcement before any recording capability is permitted to run; recording is refused if the announcement was not delivered. | VerificationAgent confirms a matching audit log row was written for this action. | Not persisted; operates on already-available call/transcript data rather than creating new stored records. |
| 38 | Do-not-call handling | medium | allowed | Available | — | Yes | Checks the scoped do-not-call list before allowing outbound follow-up actions and blocks matches. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 39 | Opt-out handling | medium | allowed | Available | — | Yes | Writes an opt-out flag to the scoped contact record and suppresses future outbound actions to it. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 40 | Emergency call exclusion | high | allowed | Available | — | Yes | Matches dialed/incoming numbers against an emergency-number list and routes them outside all automation. | VerificationAgent confirms a matching audit log row was written for this action. | Not persisted; operates on already-available call/transcript data rather than creating new stored records. |
| 41 | After-hours voicemail flow | low | allowed | Available | — | No | Evaluates the business-hours schedule and routes after-hours calls to the voicemail flow definition. | VerificationAgent confirms the response matches the normalized call/result schema. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 42 | Caller sentiment detection | low | allowed | Available | — | No | Runs local sentiment analysis over the provided transcript text. | VerificationAgent confirms the response matches the normalized call/result schema. | Not persisted; operates on already-available call/transcript data rather than creating new stored records. |
| 43 | Sales score after call | low | allowed | Available | — | No | Computes a rule-based sales score from real call/lead record data; never fabricates a score without underlying data. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 44 | Repeat caller memory | medium | allowed | Available | — | Yes | Reads prior call-log records for the matching caller from the DB-backed CRM table. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 45 | Call analytics dashboard | low | allowed | Available | — | No | Computes counts/aggregates from real scoped call-log records, or an honest empty state if none exist. | VerificationAgent confirms the response matches the normalized call/result schema. | Not persisted; operates on already-available call/transcript data rather than creating new stored records. |
| 46 | Team assignment by lead type | medium | allowed | Available | — | Yes | Matches the lead's classification to a stored team-assignment rule and writes the assignment. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 47 | Call privacy guard | high | allowed | Available | — | Yes | Every call-data query is filtered by user_id/workspace_id at the DB layer; cross-tenant reads are rejected. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 48 | Call Agent health/dependency check | low | allowed | Available | — | No | Reports process health and whether telephony/calendar/messaging providers are configured. | VerificationAgent confirms the response matches the normalized call/result schema. | Not persisted; operates on already-available call/transcript data rather than creating new stored records. |
| 49 | Call audit log | low | allowed | Available | — | No | Reads from the audit log table filtered to call-category events. | VerificationAgent confirms a matching audit log row was written for this action. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |
| 50 | Call-to-Business handoff | low | allowed | Available | — | No | Forwards the structured lead/call payload to the Business Agent and returns its response. | VerificationAgent confirms the response matches the normalized call/result schema. | Stored in the DB-backed memory/CRM table, keyed by user_id + workspace_id; never mixed across tenants. |

## Business Agent (`business`)

**Purpose:** CRM, leads, clients, pipeline, reports, campaign analytics, business intelligence.

**Example test prompt:** _Give me this week's business report: pipeline status, hot leads, and follow-ups due._

| # | Name | Risk | Permission | Status | Required integrations | Audit | Safe MVP behavior | Verification method | Memory policy |
|---|---|---|---|---|---|---|---|---|---|
| 1 | CRM contact management | low | allowed | Available | — | Yes | Operates on real DB-backed contact records if the model exists, otherwise returns an honest empty state — never fabricates contacts. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 2 | CRM deal management | medium | allowed | Available | — | Yes | Operates on real DB-backed deal records if the model exists, otherwise returns an honest empty state — never fabricates deals. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 3 | Lead tracking across channels | medium | allowed | Available | — | Yes | Aggregates real lead records tagged by source channel from the DB, or returns an honest empty state. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 4 | Lead scoring | low | allowed | Available | — | No | Computes a rule-based score from real lead record fields; never invents a score for a non-existent lead. | VerificationAgent confirms the response matches the normalized business-result schema. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 5 | Hot/cold lead tagging | low | allowed | Available | — | No | Writes a hot/cold tag derived from the real lead score to the scoped lead record. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 6 | Client records | low | allowed | Available | — | Yes | Operates on real DB-backed client records if the model exists, otherwise returns an honest empty state. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 7 | Sales pipeline stages | medium | allowed | Available | — | Yes | Reads/updates the stage field on real deal records; never fabricates pipeline movement. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 8 | Follow-up reminders | low | allowed | Available | — | No | Computes due follow-ups from real scoped records carrying a follow-up-due flag. | VerificationAgent confirms the response matches the normalized business-result schema. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 9 | Campaign performance tracking | low | allowed | Available | — | No | Aggregates real campaign metric records if present, otherwise returns an honest empty state — never fabricates performance numbers. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 10 | Google Ads lead analysis | medium | allowed | Needs external dependency | google_ads_api_credentials | No | Returns an honest capability_unavailable response until Google Ads API credentials are configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on already-stored business data rather than creating new records. |
| 11 | SEO/client report tracking | low | allowed | Available | — | No | Reads/stores real SEO report records supplied by the Browser Agent's SEO analyzer, or returns an honest empty state. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 12 | Revenue summary | medium | allowed | Available | — | Yes | Sums real invoice/deal revenue fields from the DB; returns an explicit zero/empty state rather than any fabricated revenue figure — no fake revenue. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 13 | Invoice status overview | medium | allowed | Available | — | Yes | Reads real invoice records if the model exists, otherwise returns an honest empty state. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 14 | Team task assignment | low | allowed | Available | — | Yes | Writes a real assignment record linking a task to a team member. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 15 | Project status reporting | low | allowed | Available | — | No | Reads real project status records, or returns an honest empty state if none exist. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 16 | Client health score | low | allowed | Available | — | No | Computes a rule-based score from real client activity records; never invents a score without underlying data. | VerificationAgent confirms the response matches the normalized business-result schema. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 17 | Proposal tracking | low | allowed | Available | — | Yes | Reads/writes real proposal records, or returns an honest empty state. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 18 | Won/lost deal analysis | low | allowed | Available | — | No | Aggregates real closed-deal records by outcome; returns an honest empty state if none exist. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 19 | Pipeline forecast | medium | allowed | Available | — | No | Computes a forecast strictly from real open-deal values and stage probabilities; never fabricates pipeline revenue when no deals exist — no fake revenue. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 20 | Weekly business report | low | allowed | Available | — | No | Compiles a report from real scoped records for the past week; returns an honest empty-state report rather than fabricated figures if no data exists. | VerificationAgent confirms the response matches the normalized business-result schema. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 21 | Dashboard KPI cards | low | allowed | Available | — | No | Computes KPI card values from real scoped data, defaulting to an explicit zero/empty state — never fake revenue or fake leads. | VerificationAgent confirms the response matches the normalized business-result schema. | Not persisted; operates on already-stored business data rather than creating new records. |
| 22 | Agent usage analytics | low | allowed | Available | — | No | Aggregates real agent-task/event records from the DB for the scoped workspace. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 23 | Workspace analytics | low | allowed | Available | — | No | Aggregates real scoped workspace records; returns an honest empty state if none exist. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 24 | SaaS user activity reports | medium | allowed | Available | — | Yes | Aggregates real scoped user-activity/audit records. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 25 | Churn risk detection | medium | allowed | Available | — | Yes | Applies a rule-based heuristic to real client activity/engagement records to flag risk; never flags without underlying data. | VerificationAgent confirms the response matches the normalized business-result schema. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 26 | Growth opportunity suggestions | low | allowed | Available | — | No | Generates suggestions from patterns in real scoped business records; returns an honest 'insufficient data' response if none exist. | VerificationAgent confirms the response matches the normalized business-result schema. | Not persisted; operates on already-stored business data rather than creating new records. |
| 27 | Client onboarding checklist | low | allowed | Available | — | No | Generates a checklist and tracks completion against a real client record. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 28 | Lead source attribution | low | allowed | Available | — | No | Reads the source field on real lead records to build an attribution breakdown. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 29 | Conversion rate report | low | allowed | Available | — | No | Computes conversion rate strictly from real counted lead/client records; returns 0/empty rather than a fabricated rate when no data exists. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 30 | Sales activity timeline | low | allowed | Available | — | No | Derives a timeline from real timestamped deal/activity records. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 31 | Client notes memory | low | allowed | Available | — | No | Stores/reads client notes via the DB-backed memory/CRM table. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 32 | CRM duplicate detection | medium | allowed | Available | — | Yes | Flags likely duplicate real CRM records by similarity heuristic; merge requires confirmation. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 33 | Contact import | medium | approval_required | Approval required | — | Yes | Validates and imports contact records into the DB only after explicit approval. | VerificationAgent confirms a matching audit log row was written for this action. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 34 | Contact export with approval | high | approval_required | Approval required | — | Yes | Generates a contact export file only after explicit user/Security Agent approval; sensitive fields redacted by default. | VerificationAgent confirms a matching audit log row was written for this action. | Persisted/exported only after explicit user/Security Agent approval; sensitive payloads redacted until approved. |
| 35 | Task due-date alerts | low | allowed | Available | — | No | Computes due-date alerts from real scoped task records. | VerificationAgent confirms the response matches the normalized business-result schema. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 36 | Team workload view | low | allowed | Available | — | No | Aggregates real assigned-task counts per team member. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 37 | Campaign ROI summary | medium | allowed | Available | — | No | Computes ROI strictly from real campaign spend/revenue records; returns an honest empty state instead of a fabricated ROI figure when data is missing — no fake revenue. | VerificationAgent confirms the expected row exists/changed in the scoped DB table for the user_id/workspace_id. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 38 | Monthly business review | low | allowed | Available | — | No | Compiles a review from real scoped records for the month; returns an honest empty-state review rather than fabricated performance if no data exists. | VerificationAgent confirms the response matches the normalized business-result schema. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 39 | Service package recommendation | low | allowed | Available | — | No | Matches real client profile/needs data against the stored service catalog to suggest a package. | VerificationAgent confirms the response matches the normalized business-result schema. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 40 | Client follow-up email draft | low | allowed | Available | — | No | Generates an email draft from real client/context data; sending requires separate approval via the messaging capability. | VerificationAgent confirms the response matches the normalized business-result schema. | Not persisted; operates on already-stored business data rather than creating new records. |
| 41 | Proposal outline builder | low | allowed | Available | — | No | Generates a proposal outline from real client/service data provided in the request. | VerificationAgent confirms the response matches the normalized business-result schema. | Not persisted; operates on already-stored business data rather than creating new records. |
| 42 | Business dashboard widgets | low | allowed | Available | — | No | Computes widget values from real scoped business data, defaulting to an explicit empty state — never fake revenue or fake leads. | VerificationAgent confirms the response matches the normalized business-result schema. | Not persisted; operates on already-stored business data rather than creating new records. |
| 43 | Business report export | medium | approval_required | Approval required | — | Yes | Generates the export file from real scoped report data only after explicit approval. | VerificationAgent confirms a matching audit log row was written for this action. | Persisted/exported only after explicit user/Security Agent approval; sensitive payloads redacted until approved. |
| 44 | Business-to-Memory handoff | low | allowed | Available | — | No | Forwards a structured payload to the Memory Agent and returns its response. | VerificationAgent confirms the response matches the normalized business-result schema. | Not persisted; operates on already-stored business data rather than creating new records. |
| 45 | Business-to-Workflow handoff | low | allowed | Available | — | No | Forwards a structured workflow-trigger payload to the Workflow Agent and returns its response. | VerificationAgent confirms the response matches the normalized business-result schema. | Not persisted; operates on already-stored business data rather than creating new records. |
| 46 | Business-to-Call handoff | low | allowed | Available | — | No | Forwards a structured payload to the Call Agent and returns its response. | VerificationAgent confirms the response matches the normalized business-result schema. | Not persisted; operates on already-stored business data rather than creating new records. |
| 47 | Business-to-Finance handoff | low | allowed | Available | — | No | Forwards a structured payload to the Finance Agent and returns its response. | VerificationAgent confirms the response matches the normalized business-result schema. | Not persisted; operates on already-stored business data rather than creating new records. |
| 48 | Business Agent health/dependency check | low | allowed | Available | — | No | Reports DB connectivity and whether optional CRM/ads integrations are configured. | VerificationAgent confirms the response matches the normalized business-result schema. | Not persisted; operates on already-stored business data rather than creating new records. |
| 49 | Business audit log | low | allowed | Available | — | No | Reads from the audit log table filtered to business-category events. | VerificationAgent confirms a matching audit log row was written for this action. | Stored in the DB-backed CRM/business tables, keyed by user_id + workspace_id; never mixed across tenants. |
| 50 | Business safe empty-state behavior | low | allowed | Available | — | No | Every reporting/summary code path checks for real underlying records first and returns an explicit empty-state response instead of inventing leads or revenue when none exist. | VerificationAgent confirms the response matches the normalized business-result schema. | Not persisted; operates on already-stored business data rather than creating new records. |

## Finance Agent (`finance`)

**Purpose:** Safe finance preparation: invoices, budgets, receipts, transaction drafts only, never auto-transfer.

**Example test prompt:** _Draft an invoice for client Acme Corp for $2,400 due in 30 days._

| # | Name | Risk | Permission | Status | Required integrations | Audit | Safe MVP behavior | Verification method | Memory policy |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Invoice draft creation | medium | allowed | Available | — | Yes | Generates a local invoice draft record; never submitted, sent, or charged to a payment processor. | VerificationAgent confirms the expected row exists/changed in the finance table for the scoped user_id/workspace_id. | Stored in the DB-backed finance table, keyed by user_id + workspace_id; never mixed across tenants. |
| 2 | Invoice status tracking | medium | allowed | Available | — | Yes | Reads and updates the status field of existing invoice draft records in the DB. | VerificationAgent confirms the expected row exists/changed in the finance table for the scoped user_id/workspace_id. | Stored in the DB-backed finance table, keyed by user_id + workspace_id; never mixed across tenants. |
| 3 | Payment reminder draft | medium | allowed | Available | — | Yes | Generates a reminder message draft for review; never auto-sent to the client. | VerificationAgent confirms the expected row exists/changed in the finance table for the scoped user_id/workspace_id. | Stored in the DB-backed finance table, keyed by user_id + workspace_id; never mixed across tenants. |
| 4 | Receipt OCR adapter | medium | allowed | Needs external dependency | receipt_ocr_provider | No | Returns a structured capability_unavailable response until a receipt OCR provider is configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on already-stored records rather than creating new ones. |
| 5 | Expense categorization | medium | allowed | Available | — | Yes | Applies a rule-based categorizer to expense records already stored in the DB. | VerificationAgent confirms the expected row exists/changed in the finance table for the scoped user_id/workspace_id. | Stored in the DB-backed finance table, keyed by user_id + workspace_id; never mixed across tenants. |
| 6 | Budget tracking | medium | allowed | Available | — | Yes | Computes budget-vs-actual figures from scoped DB records and stores the tracked budget definition. | VerificationAgent confirms the expected row exists/changed in the finance table for the scoped user_id/workspace_id. | Stored in the DB-backed finance table, keyed by user_id + workspace_id; never mixed across tenants. |
| 7 | Subscription tracking | medium | allowed | Available | — | Yes | Tracks recurring subscription cost records from scoped DB data. | VerificationAgent confirms the expected row exists/changed in the finance table for the scoped user_id/workspace_id. | Stored in the DB-backed finance table, keyed by user_id + workspace_id; never mixed across tenants. |
| 8 | Profit/loss summary | medium | allowed | Available | — | No | Computes a read-only profit/loss summary from scoped DB records; no transaction is executed. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored records rather than creating new ones. |
| 9 | Tax category preparation | medium | allowed | Available | — | Yes | Applies tax-category tags to existing records for later export; never files taxes. | VerificationAgent confirms the expected row exists/changed in the finance table for the scoped user_id/workspace_id. | Stored in the DB-backed finance table, keyed by user_id + workspace_id; never mixed across tenants. |
| 10 | Duplicate invoice detection | medium | allowed | Available | — | No | Flags likely duplicate invoices by similarity heuristic; read-only, no record is changed. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored records rather than creating new ones. |
| 11 | Payment draft preparation | high | allowed | Available | — | Yes | Generates a local payment draft record for review; never submits or transfers funds. | VerificationAgent confirms the expected row exists/changed in the finance table for the scoped user_id/workspace_id. | Persisted only after explicit user/Security Agent approval; sensitive payloads redacted until approved. |
| 12 | Recipient verification | high | allowed | Available | — | Yes | Cross-checks recipient details against stored vendor/client records and flags mismatches; sends nothing. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored records rather than creating new ones. |
| 13 | Amount verification | high | allowed | Available | — | Yes | Cross-checks the requested amount against invoice/budget records and flags anomalies. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored records rather than creating new ones. |
| 14 | Never auto-transfer | critical | blocked_by_default | Available | — | Yes | Hard-coded refusal: no code path in this agent ever initiates a real fund transfer; every transfer request returns blocked_by_default regardless of configuration or plan tier. | VerificationAgent confirms the request was rejected and no transfer/credential-store call was ever attempted. | Never persisted anywhere, under any configuration; the refusal itself is written only to the audit log. |
| 15 | Never store bank credentials | critical | blocked_by_default | Available | — | Yes | Hard-coded refusal: bank account/routing numbers are rejected before reaching any storage layer, regardless of configuration. | VerificationAgent confirms the request was rejected and no transfer/credential-store call was ever attempted. | Never persisted anywhere, under any configuration; the refusal itself is written only to the audit log. |
| 16 | Never store card credentials | critical | blocked_by_default | Available | — | Yes | Hard-coded refusal: card number/CVV/expiry fields are rejected before reaching any storage layer, regardless of configuration. | VerificationAgent confirms the request was rejected and no transfer/credential-store call was ever attempted. | Never persisted anywhere, under any configuration; the refusal itself is written only to the audit log. |
| 17 | SecurityAgent mandatory for financial actions | critical | allowed | Available | — | Yes | Every capability in this agent classified as a financial action is routed through Security Agent for approval before any execution step; there is no bypass flag. | VerificationAgent confirms a matching audit log row was written for this action. | Persisted only as an audit-log/approval reference; no raw financial payload is stored outside the approval record. |
| 18 | Approval flow for payment draft | high | approval_required | Approval required | — | Yes | Creates a Security Agent approval request for a prepared payment draft; the draft is never sent unless approval is granted. | VerificationAgent confirms a matching audit log row was written for this action. | Persisted only after explicit user/Security Agent approval; sensitive payloads redacted until approved. |
| 19 | Refund draft only | high | allowed | Available | — | Yes | Generates a local refund draft for review; never submits or executes an actual refund. | VerificationAgent confirms the expected row exists/changed in the finance table for the scoped user_id/workspace_id. | Stored in the DB-backed finance table, keyed by user_id + workspace_id; never mixed across tenants. |
| 20 | Cashflow report | medium | allowed | Available | — | No | Computes a read-only cashflow report from scoped DB records. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored records rather than creating new ones. |
| 21 | Monthly expense alerts | medium | allowed | Available | — | Yes | Generates an informational alert when monthly expenses cross a configured threshold; nothing is charged. | VerificationAgent confirms response matches the normalized result schema. | Held only for the lifetime of the active request; not persisted to durable storage. |
| 22 | Budget overspend alert | medium | allowed | Available | — | Yes | Generates an informational alert when tracked spend exceeds the defined budget; nothing is charged. | VerificationAgent confirms response matches the normalized result schema. | Held only for the lifetime of the active request; not persisted to durable storage. |
| 23 | Finance dashboard | medium | allowed | Available | — | No | Computes read-only counts/aggregates from scoped finance records. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored records rather than creating new ones. |
| 24 | Invoice aging report | medium | allowed | Available | — | No | Computes a read-only aging report from scoped invoice records. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored records rather than creating new ones. |
| 25 | Export reports with approval | high | approval_required | Approval required | — | Yes | Generates an export file of scoped financial reports only after explicit approval; sensitive fields redacted by default. | VerificationAgent confirms a matching audit log row was written for this action. | Persisted only after explicit user/Security Agent approval; sensitive payloads redacted until approved. |
| 26 | Vendor list | medium | allowed | Available | — | Yes | Maintains a scoped list of vendor records in the DB. | VerificationAgent confirms the expected row exists/changed in the finance table for the scoped user_id/workspace_id. | Stored in the DB-backed finance table, keyed by user_id + workspace_id; never mixed across tenants. |
| 27 | Client billing summary | medium | allowed | Available | — | No | Computes a read-only billing summary for a client from scoped invoice records. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored records rather than creating new ones. |
| 28 | Recurring invoice draft | medium | allowed | Available | — | Yes | Generates a recurring invoice draft/schedule for review; drafts are never auto-sent or auto-charged. | VerificationAgent confirms the expected row exists/changed in the finance table for the scoped user_id/workspace_id. | Stored in the DB-backed finance table, keyed by user_id + workspace_id; never mixed across tenants. |
| 29 | Late payment reminder | medium | allowed | Available | — | Yes | Generates a late-payment reminder draft for review; never auto-sent. | VerificationAgent confirms the expected row exists/changed in the finance table for the scoped user_id/workspace_id. | Stored in the DB-backed finance table, keyed by user_id + workspace_id; never mixed across tenants. |
| 30 | Expense duplicate detection | medium | allowed | Available | — | No | Flags likely duplicate expenses by similarity heuristic; read-only, no record is changed. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored records rather than creating new ones. |
| 31 | Merchant categorization | medium | allowed | Available | — | Yes | Applies a rule-based merchant categorizer to expense records already stored in the DB. | VerificationAgent confirms the expected row exists/changed in the finance table for the scoped user_id/workspace_id. | Stored in the DB-backed finance table, keyed by user_id + workspace_id; never mixed across tenants. |
| 32 | Tax preparation summary | medium | allowed | Available | — | No | Computes a read-only tax-relevant totals summary from scoped, tagged records. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored records rather than creating new ones. |
| 33 | Safe finance memory | medium | allowed | Available | — | Yes | Persists finance records through the DB-backed memory layer with sensitive fields (credentials, full account numbers) redacted before storage. | VerificationAgent confirms the expected row exists/changed in the finance table for the scoped user_id/workspace_id. | Stored in the DB-backed finance table, keyed by user_id + workspace_id; never mixed across tenants. |
| 34 | Approved recurring rules | high | approval_required | Approval required | — | Yes | Stores recurring-action rules only after explicit Security Agent approval; enforces the approved scope on every subsequent recurring draft. | VerificationAgent confirms a matching audit log row was written for this action. | Persisted only after explicit user/Security Agent approval; sensitive payloads redacted until approved. |
| 35 | Payment risk scoring | high | allowed | Available | — | Yes | Scores a prepared payment/invoice draft for risk signals (mismatched recipient, unusual amount) before it reaches approval. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored records rather than creating new ones. |
| 36 | Fake invoice detection handoff | high | allowed | Available | — | Yes | Flags a suspected fraudulent invoice by heuristic and hands the flagged case off to the Security Agent for review. | VerificationAgent confirms a matching audit log row was written for this action. | Stored in the DB-backed finance table, keyed by user_id + workspace_id; never mixed across tenants. |
| 37 | Finance audit log | medium | allowed | Available | — | No | Reads from the audit log table filtered to finance-category events; read-only. | VerificationAgent confirms a matching audit log row was written for this action. | Stored in the DB-backed finance table, keyed by user_id + workspace_id; never mixed across tenants. |
| 38 | Finance report PDF/export adapter | medium | allowed | Needs external dependency | pdf_export_engine | No | Returns a structured capability_unavailable response until a PDF export engine is configured. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on already-stored records rather than creating new ones. |
| 39 | Subscription renewal alert | medium | allowed | Available | — | Yes | Generates an informational alert ahead of a tracked subscription's renewal date; nothing is charged. | VerificationAgent confirms response matches the normalized result schema. | Held only for the lifetime of the active request; not persisted to durable storage. |
| 40 | Cancellation reminder | medium | allowed | Available | — | Yes | Generates an informational cancellation reminder ahead of a tracked renewal date; nothing is cancelled automatically. | VerificationAgent confirms response matches the normalized result schema. | Held only for the lifetime of the active request; not persisted to durable storage. |
| 41 | Finance-to-Business handoff | medium | allowed | Available | — | Yes | Hands off scoped finance summary data to the Business Agent for cross-agent reporting. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored records rather than creating new ones. |
| 42 | Finance-to-Security handoff | high | allowed | Available | — | Yes | Hands off a financial action payload to the Security Agent for approval before any execution step. | VerificationAgent confirms a matching audit log row was written for this action. | Persisted only after explicit user/Security Agent approval; sensitive payloads redacted until approved. |
| 43 | Finance-to-Memory handoff | medium | allowed | Available | — | Yes | Hands off redacted finance records to the Memory Agent for durable, scoped storage. | VerificationAgent confirms the expected row exists/changed in the finance table for the scoped user_id/workspace_id. | Stored in the DB-backed finance table, keyed by user_id + workspace_id; never mixed across tenants. |
| 44 | Financial action proof handoff | high | allowed | Available | — | Yes | Attaches a verifiable proof record (audit log reference) to every completed financial action before handoff to the Verification Agent. | VerificationAgent confirms a matching audit log row was written for this action. | Stored in the DB-backed finance table, keyed by user_id + workspace_id; never mixed across tenants. |
| 45 | Finance role/plan access control | medium | allowed | Available | — | Yes | Checks the caller's role and subscription plan against finance capability ACLs before executing. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored records rather than creating new ones. |
| 46 | Finance data isolation | high | allowed | Available | — | Yes | Every finance query is filtered by user_id + workspace_id at the DB layer; cross-tenant reads are rejected. | VerificationAgent confirms the expected row exists/changed in the finance table for the scoped user_id/workspace_id. | Stored in the DB-backed finance table, keyed by user_id + workspace_id; never mixed across tenants. |
| 47 | Finance empty-state behavior | low | allowed | Available | — | No | Returns a structured empty-state result (not an error) when no finance records exist for the scope. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored records rather than creating new ones. |
| 48 | Finance Agent health/dependency check | low | allowed | Available | — | No | Reports DB connectivity and whether optional OCR/PDF-export adapters are configured. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; operates on already-stored records rather than creating new ones. |
| 49 | Finance structured unavailable for payment APIs | medium | allowed | Available | — | No | Returns a structured capability_unavailable response (never a silent failure or fake success) whenever a real payment-processor API call is requested. | N/A while capability_status is external_dependency_required or planned; verification is skipped and surfaced as such. | Not persisted; operates on already-stored records rather than creating new ones. |
| 50 | Finance no-autopay enforcement test | critical | allowed | Available | — | Yes | Runs an automated self-test confirming that auto-transfer, bank-credential storage, and card-credential storage are all refused as expected; fails loudly if any refusal is bypassed. | VerificationAgent confirms the self-test result recorded all three refusal checks as passed. | Persisted as a self-test result record in the finance table for the audit trail. |

## Creator Agent (`creator`)

**Purpose:** Content/video production brain: scripts, VEO prompts, captions, thumbnails, calendars, campaign creative.

**Example test prompt:** _Write a 30-second video ad script for our new product launch with 3 hook variations._

| # | Name | Risk | Permission | Status | Required integrations | Audit | Safe MVP behavior | Verification method | Memory policy |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Video ad scripts | low | allowed | Available | — | No | Generates a text video-ad script using the configured LLM; returned as plain text, never rendered or published. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 2 | Short-form hooks | low | allowed | Available | — | No | Generates a list of candidate hook lines as text; nothing is filmed or published. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 3 | VEO 3 prompts | low | allowed | Available | — | No | Generates a text VEO 3 prompt; returned to the caller, no video is rendered by this agent. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 4 | VEO JSON prompts | low | allowed | Available | — | No | Generates a schema-validated VEO JSON prompt object; returned to the caller, no video is rendered. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 5 | Scene-by-scene storyboards | low | allowed | Available | — | No | Generates a scene-by-scene text storyboard from an input script; no image/video is rendered. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 6 | Voiceover timing | low | allowed | Available | — | No | Computes estimated per-line voiceover timing from script text using a words-per-minute heuristic; no audio is rendered. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 7 | Captions/subtitles | low | allowed | Available | — | No | Generates caption/subtitle text from a script or provided transcript; returned as plain text. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 8 | Thumbnail concepts | low | allowed | Available | — | No | Generates text descriptions of thumbnail concepts (layout, text overlay, focal point); no image is rendered. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 9 | Retention editing plan | low | allowed | Available | — | No | Generates a text editing plan (pacing, cut points, pattern interrupts) from an input script; no video is edited. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 10 | Content calendar | low | allowed | Available | — | No | Generates a draft content calendar as structured JSON for the requested period; not persisted unless separately saved. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 11 | Brand style matching | medium | allowed | Available | — | No | Recalls stored brand tone/style rules from the Memory Agent and applies them to generated copy. | VerificationAgent confirms response matches the normalized result schema. | Stored in the DB-backed content/memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 12 | CGI/motion ad planning | low | allowed | Available | — | No | Generates a text planning document describing CGI/motion ad concepts and shot intent; no video is rendered. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 13 | A/B hook variations | low | allowed | Available | — | No | Generates multiple candidate hook variations as text for the caller to A/B test manually. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 14 | YouTube title pack | low | allowed | Available | — | No | Generates a set of candidate YouTube titles as text; nothing is uploaded or published. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 15 | YouTube thumbnail pack | low | allowed | Available | — | No | Generates a set of candidate thumbnail concept descriptions as text; no image is rendered or uploaded. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 16 | Script split into 8-second scenes | low | allowed | Available | — | No | Splits input script text into approximately 8-second scene segments for downstream video-prompt generation. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 17 | Creator asset manager | medium | allowed | Available | — | Yes | Tracks references to generated content assets (scripts, prompts, copy) in the DB-backed asset table. | VerificationAgent confirms the expected row exists/changed in the content table for the scoped user_id/workspace_id. | Stored in the DB-backed content/memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 18 | Shot list generation | low | allowed | Available | — | No | Generates a text shot list from an input script or storyboard; no filming occurs. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 19 | B-roll plan | low | allowed | Available | — | No | Generates a text list of suggested B-roll shots/themes tied to a script; no footage is sourced or filmed. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 20 | Music/SFX notes | low | allowed | Available | — | No | Generates text notes suggesting music mood and SFX cue points for a script; no audio is sourced or rendered. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 21 | Publishing checklist | medium | allowed | Available | — | Yes | Generates a pre-publish checklist (captions, thumbnail, compliance, approval status) for review. Do not publish automatically. Publishing requires approval. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 22 | Approval before publishing | medium | approval_required | Approval required | — | Yes | Creates an explicit approval request before any content is marked publish-ready. Do not publish automatically. Publishing requires approval. | VerificationAgent confirms a matching audit log row was written for this action. | Persisted/released only after explicit user approval; nothing is marked publish-ready until approved. |
| 23 | Competitor content analysis | medium | allowed | Available | — | No | Summarizes competitor content patterns from input/handoff data supplied by the Browser Agent; does not scrape live sites itself. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 24 | CTA variations | low | allowed | Available | — | No | Generates multiple candidate call-to-action lines as text. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 25 | Offer angle generation | low | allowed | Available | — | No | Generates multiple candidate offer angles as text from a product/service brief. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 26 | Multi-platform repurposing | low | allowed | Available | — | No | Reformats existing generated content into platform-specific variant text (length/tone/format); nothing is posted. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 27 | Instagram caption | low | allowed | Available | — | No | Generates Instagram-style caption text; returned to the caller, nothing is posted to Instagram. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 28 | TikTok caption | low | allowed | Available | — | No | Generates TikTok-style caption text; returned to the caller, nothing is posted to TikTok. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 29 | YouTube Shorts plan | low | allowed | Available | — | No | Generates a text plan (hook, scenes, caption) for a YouTube Shorts video; nothing is uploaded. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 30 | Facebook ad copy | low | allowed | Available | — | No | Generates Facebook-style ad copy text; returned to the caller, no ad is submitted to Meta. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 31 | Google Ads creative text | low | allowed | Available | — | No | Generates Google Ads-style headline/description text within character-limit constraints; no ad is submitted. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 32 | Landing page hero copy | low | allowed | Available | — | No | Generates landing-page hero headline/subheadline/CTA text; returned to the caller, no page is published. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 33 | Email campaign copy | low | allowed | Available | — | No | Generates email subject line and body copy as text; no email is sent. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 34 | Brand tone memory | medium | allowed | Available | — | Yes | Reads/writes brand tone and style rules via the DB-backed memory layer, scoped to the workspace. | VerificationAgent confirms the expected row exists/changed in the content table for the scoped user_id/workspace_id. | Stored in the DB-backed content/memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 35 | Content idea generator | low | allowed | Available | — | No | Generates a list of candidate content ideas as text from a topic/niche brief. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 36 | Monthly content calendar | medium | allowed | Available | — | Yes | Generates a monthly content calendar and persists it as a scoped calendar record for later recall. | VerificationAgent confirms the expected row exists/changed in the content table for the scoped user_id/workspace_id. | Stored in the DB-backed content/memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 37 | Product launch script | low | allowed | Available | — | No | Generates a product-launch announcement script as text. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 38 | Explainer video outline | low | allowed | Available | — | No | Generates a structured outline (problem, solution, demo, CTA) for an explainer video script. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 39 | UGC ad script | low | allowed | Available | — | No | Generates a UGC-style ad script as text intended for a creator/actor to perform. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 40 | Testimonial ad script | medium | allowed | Available | — | Yes | Generates a testimonial-style ad script template as text; any real customer testimonial content supplied by the caller is treated as sensitive input and is not stored beyond the request. | VerificationAgent confirms response matches the normalized result schema. | Held only for the lifetime of the active generation request; not persisted to durable storage. |
| 41 | Before/after ad concept | low | allowed | Available | — | No | Generates a text before/after ad concept (framing, copy, visual beats); no image/video is rendered. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 42 | Storytelling framework | low | allowed | Available | — | No | Generates a text storytelling framework (e.g. problem-agitate-solve) applied to the campaign brief. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 43 | Hook scoring | low | allowed | Available | — | No | Scores generated hooks using a local heuristic (length, curiosity gap, specificity); no external analytics platform is called. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 44 | Creative compliance check | medium | allowed | Available | — | Yes | Runs a rule-based compliance check (banned claims, platform policy keywords) against generated copy before it reaches the publishing checklist. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 45 | Creator-to-Visual handoff | medium | allowed | Available | — | Yes | Hands off approved creative briefs/scripts to the Visual Agent for asset production. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 46 | Creator-to-Browser research handoff | medium | allowed | Available | — | Yes | Hands off a research request (e.g. competitor pages) to the Browser Agent; the Creator Agent never scrapes directly. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 47 | Creator-to-Business campaign handoff | medium | allowed | Available | — | Yes | Hands off finalized, approved campaign creative to the Business Agent for campaign scheduling/tracking. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 48 | Creator Agent health/dependency check | low | allowed | Available | — | No | Reports LLM provider connectivity and whether optional video-rendering/publishing adapters are configured. | VerificationAgent confirms response matches the normalized result schema. | Not persisted; the response is generated on demand and returned directly to the caller. |
| 49 | Creator audit log | low | allowed | Available | — | Yes | Reads from the audit log table filtered to creator-category events. | VerificationAgent confirms a matching audit log row was written for this action. | Stored in the DB-backed content/memory table, keyed by user_id + workspace_id; never mixed across tenants. |
| 50 | No auto-publishing enforcement | medium | approval_required | Available | — | Yes | Hard-enforces that no content is ever published automatically; every publish action requires explicit approval before execution. Do not publish automatically. Publishing requires approval. | VerificationAgent confirms no publish action executed without a corresponding approval record. | Persisted/released only after explicit user approval; nothing is marked publish-ready until approved. |
